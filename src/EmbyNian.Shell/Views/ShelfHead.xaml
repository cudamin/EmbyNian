<?xml version="1.0" encoding="utf-8"?>
<!-- 货架标题：短强调线建立阅读起点，数量收在轻胶囊里，控件仍留在右端。 -->
<UserControl
    x:Class="EmbyNian.Shell.Views.ShelfHead"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="ShelfNoteStyle" TargetType="Border">
            <Setter Property="Background" Value="{ThemeResource EgSurfaceAltBrush}" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="10,4" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid ColumnSpacing="12" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border
                Width="3"
                Height="20"
                Background="{ThemeResource EgAccentBrush}"
                CornerRadius="2"
                VerticalAlignment="Center" />
            <TextBlock
                x:Name="TitleText"
                Grid.Column="1"
                MaxLines="1"
                Style="{StaticResource EgSectionTitleStyle}"
                TextTrimming="CharacterEllipsis"
                VerticalAlignment="Center" />
        </Grid>

        <StackPanel
            Grid.Column="1"
            Margin="16,0,0,0"
            Orientation="Horizontal"
            VerticalAlignment="Center">
            <Border x:Name="NoteCapsule" Style="{StaticResource ShelfNoteStyle}" Visibility="Collapsed">
                <TextBlock
                    x:Name="NoteText"
                    Style="{StaticResource EgDataStyle}"
                    VerticalAlignment="Center" />
            </Border>
            <ContentPresenter
                x:Name="TrailingHost"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                Visibility="Collapsed" />
        </StackPanel>
        <Border
            x:Name="Rule"
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="0,12,0,0"
            Opacity="0.45"
            Style="{StaticResource EgRuleStyle}" />
    </Grid>
</UserControl>
