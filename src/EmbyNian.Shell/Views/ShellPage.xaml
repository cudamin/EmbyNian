<?xml version="1.0" encoding="utf-8"?>
<!--
  The shell: the two chrome rows, the breadcrumb, and the frame every page lives in.

  This one file replaces four WinForms types — NavigationRail (188 hand-painted lines), the
  breadcrumb strip, the search box row and MainForm's manual page swapping — and fixes by
  construction the note left at MainForm.cs:422, that 搜索 and 服务器 had to be listed by hand
  because neither had a rail entry.

  **2026-09-06：左边那条 NavigationView 删掉了**（「删掉侧边栏」），换成第 1 行那条横向标签栏；**2026-09-08 那条
  标签栏也删掉了**（「移除…主页、电视节目、电影这一栏」）。所以「每个目的地都是一个 NavigationViewItem」那句旧话
  早已不成立 —— 现在媒体库的入口是主页那一排「媒体库」卡片，回主页是标题栏上那颗「主页」按钮，而键盘次序、
  读屏名字、返回栈仍然只有这一份来源。搜索没有自己的按钮之外的位置：它是标题栏上那颗按钮开的一页
  （「直接在当前窗口跳转」）。

  The sign-in card is a sibling of the browse container rather than a page inside the frame: one
  window, one theme root, one XamlRoot, and signing out shows a control that is already built
  instead of rebuilding the tree.

  Deliberately no transitions anywhere (requirement 9: no fading, show on hover immediately).

  RequestedTheme 不写在这里：一套浅色主题要靠把这棵树翻成 ElementTheme.Light 才能让框架自己那几百个
  没被 Palette.xaml 覆盖的刷子跟着走，所以深浅由 ThemeHost.Register 在构造时按当前主题设，换主题时再
  改一次。写死 Dark 等于让浅色主题只换一半。
-->
<UserControl
    x:Class="EmbyNian.Shell.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EmbyNian.Shell.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource EgUiFontFamily}"
    FontSize="{StaticResource EgBodyFontSize}">

    <!--
      外壳一行，浮在页面上面，32 像素高（<c>ChromeHeight</c>）：

        主页 · 设置 · 搜索 · 后退 · 前进 ┄┄ 拖动区 ┄┄ 账号 ┄┄ 系统那三颗窗口按钮
        ────────────────────────────────────────────────────────────────────
        页面（从窗口最左边开始，一条侧边栏都没有）

      **左边那条 NavigationView 在 2026-09-06 整个删掉了**（「删掉侧边栏」），换成了顶部标签栏；**那条标签栏又在
      2026-09-08 按「移除…主页、电视节目、电影这一栏」删掉了**；**2026-09-09 起它空出来的第 1 行也删掉了** ——
      标签删了、那一行还留着 48 像素，屏上就是一条谁也不用的空带，这正是「删掉之后原处留一个空位」的病根。
      所以媒体库的入口回到主页那一排「媒体库」卡片（点卡进库），回主页走标题栏上那颗「主页」按钮，账号那颗
      按钮收成一行挪进标题栏右端 —— 右上角是 Emby 网页版、Plex 放它的地方（它 09-06 从侧边栏最底下挪来，
      右上角是当时从两个位置里挑的这一个）。

      **这一行不上底色。** 页面自己那一块底色由 ContentHost 涂，主页上则是那张铺满的剧照本身 —— 一条实心条
      压在大图上就是图顶上一道黑边，那正是「红框框出来的地方全填充上海报」要去掉的东西。压在图上那一档的墨走
      不随主题走的浅墨（见 PaintTitleInk），底是轮播自己顶上那层 120 高的黑色渐变，32 装得进 120。

      叠放次序写成 Canvas.ZIndex 而不是靠声明顺序：这几块的上下关系是「页面 → 外壳 → 登录卡 → 播放器 →
      通知」，写成数字，读这个文件的人不用在心里数元素的先后。
    -->
    <Grid x:Name="Root">
        <!-- 外壳那一行。整块一起收起（播放时），所以是一个 Grid：播放层要整个窗口，而「外壳收一半」是
             没有意义的中间态。 -->
        <Grid
            x:Name="Chrome"
            Canvas.ZIndex="10"
            VerticalAlignment="Top"
            Background="Transparent">

        <!-- App-drawn title bar, laid out the way the Claude app lays the same strip out: a flat row of icon
             buttons — 设置, 搜索, 后退, 前进 — with the two arrows dimmed rather than removed when there is
             nowhere to go.

             需求 1 的第一句是「去掉窗口标题的 EmbyNian」, so the word is gone; the green mark that used to sit
             at the left end went with 「左上角这个绿标移除掉」. The indent stays, because the strip's left end
             has to remain something to drag the window by.

             底色是 Transparent：这一条压在页面上面，底由页面自己出（ContentHost 的底色，主页上则是大图本身），
             所以这一条只出按钮，不出底。

             The right side is reserved for the OS caption buttons; everything else is declared draggable
             non-client area by HostWindow except this row, which it cuts a passthrough hole for. That hole
             is not decoration: a button drawn inside a caption region never receives a click at all, because
             the frame claims the point before XAML sees it and Windows turns the press into a window drag.
             ShellPage measures TitleActions and reports the rectangle, so the hole is always where the
             buttons actually are rather than where this markup implies they are — which is also why all four
             sit in one panel and not in two: one panel is one rectangle.

             **账号那颗按钮也站在这一行里**（2026-09-09 从已删掉的第 1 行挪进来），所以窗口挖的是两个洞：
             这一排一个、账号一个（见 ReportTitleBarHoles）—— 账号那块不挖洞的话，按下去同样是拖窗口。 -->
        <Grid
            x:Name="AppTitleBar"
            Height="32"
            Padding="0,0,148,0"
            Background="Transparent">
            <!-- 只保留图标，没有按钮底板。交互区域仍是标准 Button，键盘焦点和标题栏穿透区域不变。 -->
            <Grid.Resources>
                <Style BasedOn="{StaticResource DefaultButtonStyle}" TargetType="Button">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="CornerRadius" Value="8" />
                    <Setter Property="Height" Value="30" />
                    <Setter Property="Width" Value="42" />
                    <Setter Property="Padding" Value="0" />
                </Style>
                <Style TargetType="PathIcon">
                    <Setter Property="Width" Value="24" />
                    <Setter Property="Height" Value="24" />
                </Style>
            </Grid.Resources>

            <!-- 所有路径重新按 24×24 绘制。圆头线段用填充轮廓表达，F1 合并交叠笔画；
                 图标继承 Button 前景，主题、高对比度与停用态仍由 PaintTitleInk 统一管理。 -->
            <StackPanel
                x:Name="TitleActions"
                Margin="18,0,0,0"
                Orientation="Horizontal"
                Spacing="2"
                VerticalAlignment="Center"
                HorizontalAlignment="Left">
                <Button x:Name="HomeButton" x:Uid="ShellPage_HomeButton" Click="OnHomeClicked">
                    <PathIcon Data="F1 M 2.92,9.61 L 11.42,2.51 A 0.9,0.9 0 0 1 12.58,3.89 L 4.08,10.99 A 0.9,0.9 0 0 1 2.92,9.61 Z M 12.58,2.51 L 21.08,9.61 A 0.9,0.9 0 0 1 19.92,10.99 L 11.42,3.89 A 0.9,0.9 0 0 1 12.58,2.51 Z M 6.1,9.2 L 6.1,20 A 0.9,0.9 0 0 1 4.3,20 L 4.3,9.2 A 0.9,0.9 0 0 1 6.1,9.2 Z M 5.2,19.1 L 9.5,19.1 A 0.9,0.9 0 0 1 9.5,20.9 L 5.2,20.9 A 0.9,0.9 0 0 1 5.2,19.1 Z M 8.6,20 L 8.6,14.5 A 0.9,0.9 0 0 1 10.4,14.5 L 10.4,20 A 0.9,0.9 0 0 1 8.6,20 Z M 9.5,13.6 L 14.5,13.6 A 0.9,0.9 0 0 1 14.5,15.4 L 9.5,15.4 A 0.9,0.9 0 0 1 9.5,13.6 Z M 15.4,14.5 L 15.4,20 A 0.9,0.9 0 0 1 13.6,20 L 13.6,14.5 A 0.9,0.9 0 0 1 15.4,14.5 Z M 14.5,19.1 L 18.8,19.1 A 0.9,0.9 0 0 1 18.8,20.9 L 14.5,20.9 A 0.9,0.9 0 0 1 14.5,19.1 Z M 17.9,20 L 17.9,9.2 A 0.9,0.9 0 0 1 19.7,9.2 L 19.7,20 A 0.9,0.9 0 0 1 17.9,20 Z" />
                </Button>
                <Button x:Name="SettingsButton" x:Uid="ShellPage_SettingsButton" Click="OnSettingsClicked">
                    <PathIcon Data="F1 M 8.14,5.11 L 9.09,2.46 A 0.83,0.83 0 0 1 10.64,3.02 L 9.69,5.66 A 0.83,0.83 0 0 1 8.14,5.11 Z M 9.86,1.92 L 14.14,1.92 A 0.83,0.83 0 0 1 14.14,3.57 L 9.86,3.57 A 0.83,0.83 0 0 1 9.86,1.92 Z M 14.91,2.46 L 15.86,5.11 A 0.83,0.83 0 0 1 14.31,5.66 L 13.36,3.02 A 0.83,0.83 0 0 1 14.91,2.46 Z M 15.5,4.67 L 16.6,5.31 A 0.83,0.83 0 0 1 15.77,6.73 L 14.67,6.1 A 0.83,0.83 0 0 1 15.5,4.67 Z M 16.04,5.21 L 18.8,4.71 A 0.83,0.83 0 0 1 19.09,6.33 L 16.33,6.83 A 0.83,0.83 0 0 1 16.04,5.21 Z M 19.66,5.11 L 21.8,8.81 A 0.83,0.83 0 0 1 20.37,9.63 L 18.23,5.93 A 0.83,0.83 0 0 1 19.66,5.11 Z M 21.71,9.76 L 19.9,11.9 A 0.83,0.83 0 0 1 18.64,10.83 L 20.46,8.69 A 0.83,0.83 0 0 1 21.71,9.76 Z M 20.1,11.36 L 20.1,12.64 A 0.83,0.83 0 0 1 18.45,12.64 L 18.45,11.36 A 0.83,0.83 0 0 1 20.1,11.36 Z M 19.9,12.1 L 21.71,14.24 A 0.83,0.83 0 0 1 20.46,15.31 L 18.64,13.17 A 0.83,0.83 0 0 1 19.9,12.1 Z M 21.8,15.19 L 19.66,18.89 A 0.83,0.83 0 0 1 18.23,18.07 L 20.37,14.37 A 0.83,0.83 0 0 1 21.8,15.19 Z M 18.8,19.29 L 16.04,18.79 A 0.83,0.83 0 0 1 16.33,17.17 L 19.09,17.67 A 0.83,0.83 0 0 1 18.8,19.29 Z M 16.6,18.69 L 15.5,19.33 A 0.83,0.83 0 0 1 14.67,17.9 L 15.77,17.27 A 0.83,0.83 0 0 1 16.6,18.69 Z M 15.86,18.89 L 14.91,21.54 A 0.83,0.83 0 0 1 13.36,20.98 L 14.31,18.34 A 0.83,0.83 0 0 1 15.86,18.89 Z M 14.14,22.08 L 9.86,22.08 A 0.83,0.83 0 0 1 9.86,20.43 L 14.14,20.43 A 0.83,0.83 0 0 1 14.14,22.08 Z M 9.09,21.54 L 8.14,18.89 A 0.83,0.83 0 0 1 9.69,18.34 L 10.64,20.98 A 0.83,0.83 0 0 1 9.09,21.54 Z M 8.5,19.33 L 7.4,18.69 A 0.83,0.83 0 0 1 8.23,17.27 L 9.33,17.9 A 0.83,0.83 0 0 1 8.5,19.33 Z M 7.96,18.79 L 5.2,19.29 A 0.83,0.83 0 0 1 4.91,17.67 L 7.67,17.17 A 0.83,0.83 0 0 1 7.96,18.79 Z M 4.34,18.89 L 2.2,15.19 A 0.83,0.83 0 0 1 3.63,14.37 L 5.77,18.07 A 0.83,0.83 0 0 1 4.34,18.89 Z M 2.29,14.24 L 4.1,12.1 A 0.83,0.83 0 0 1 5.36,13.17 L 3.54,15.31 A 0.83,0.83 0 0 1 2.29,14.24 Z M 3.9,12.64 L 3.9,11.36 A 0.83,0.83 0 0 1 5.55,11.36 L 5.55,12.64 A 0.83,0.83 0 0 1 3.9,12.64 Z M 4.1,11.9 L 2.29,9.76 A 0.83,0.83 0 0 1 3.54,8.69 L 5.36,10.83 A 0.83,0.83 0 0 1 4.1,11.9 Z M 2.2,8.81 L 4.34,5.11 A 0.83,0.83 0 0 1 5.77,5.93 L 3.63,9.63 A 0.83,0.83 0 0 1 2.2,8.81 Z M 5.2,4.71 L 7.96,5.21 A 0.83,0.83 0 0 1 7.67,6.83 L 4.91,6.33 A 0.83,0.83 0 0 1 5.2,4.71 Z M 7.4,5.31 L 8.5,4.67 A 0.83,0.83 0 0 1 9.33,6.1 L 8.23,6.73 A 0.83,0.83 0 0 1 7.4,5.31 Z M 12,8.28 A 3.72,3.72 0 1 1 12,15.73 A 3.72,3.72 0 1 1 12,8.28 Z M 12,9.93 A 2.08,2.08 0 1 0 12,14.08 A 2.08,2.08 0 1 0 12,9.93 Z" />
                </Button>
                <Button x:Name="SearchButton" x:Uid="ShellPage_SearchButton" Click="OnSearchClicked">
                    <PathIcon Data="F1 M 10.25,2.75 A 7.5,7.5 0 1 1 10.25,17.75 A 7.5,7.5 0 1 1 10.25,2.75 Z M 10.25,4.55 A 5.7,5.7 0 1 0 10.25,15.95 A 5.7,5.7 0 1 0 10.25,4.55 Z M 16.19,14.91 L 21.64,20.36 A 0.9,0.9 0 0 1 20.36,21.64 L 14.91,16.19 A 0.9,0.9 0 0 1 16.19,14.91 Z" />
                </Button>
                <Button x:Name="BackButton" x:Uid="ShellPage_BackButton" Click="OnBackClicked" IsEnabled="False">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Left" Modifiers="Menu" />
                    </Button.KeyboardAccelerators>
                    <PathIcon Data="F1 M 11.74,5.73 L 4.94,12.63 A 0.9,0.9 0 0 1 3.66,11.37 L 10.46,4.47 A 0.9,0.9 0 0 1 11.74,5.73 Z M 4.94,11.37 L 11.74,18.27 A 0.9,0.9 0 0 1 10.46,19.53 L 3.66,12.63 A 0.9,0.9 0 0 1 4.94,11.37 Z M 4.6,11.1 L 20.3,11.1 A 0.9,0.9 0 0 1 20.3,12.9 L 4.6,12.9 A 0.9,0.9 0 0 1 4.6,11.1 Z" />
                </Button>
                <Button x:Name="ForwardButton" x:Uid="ShellPage_ForwardButton" Click="OnForwardClicked" IsEnabled="False">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Right" Modifiers="Menu" />
                    </Button.KeyboardAccelerators>
                    <PathIcon Data="F1 M 13.54,4.47 L 20.34,11.37 A 0.9,0.9 0 0 1 19.06,12.63 L 12.26,5.73 A 0.9,0.9 0 0 1 13.54,4.47 Z M 20.34,12.63 L 13.54,19.53 A 0.9,0.9 0 0 1 12.26,18.27 L 19.06,11.37 A 0.9,0.9 0 0 1 20.34,12.63 Z M 3.7,11.1 L 19.4,11.1 A 0.9,0.9 0 0 1 19.4,12.9 L 3.7,12.9 A 0.9,0.9 0 0 1 3.7,11.1 Z" />
                </Button>
            </StackPanel>
        </Grid>

        <!-- 账号：用户名、服务器名，加一张切换服务器／切换用户／注销的菜单。2026-09-06 从侧边栏最底下挪到
             顶上那一排的下一行右端；2026-09-09 第 1 行删掉之后挪进标题栏这一行 —— 右上角还是 Emby 网页版、
             Plex 放它的地方，挪进来的代价是收成一行：用户名和服务器名排成一行，窗口拖窄了截字（MaxWidth 240）。
             它站在窗口算成标题栏的那 32 像素里，所以得像左边那五颗一样挖洞（见 ReportTitleBarHoles）。

             账号是 AppTitleBar 的兄弟，尺寸随内容变化；两边共享 Chrome 的透明按钮状态资源。 -->
        <Button
            x:Name="AccountButton"
            Margin="0,0,160,0"
            Padding="10,3"
            MaxWidth="240"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            HorizontalContentAlignment="Left"
            Background="Transparent"
            BorderThickness="0"
            CornerRadius="{StaticResource EgCardCornerRadius}"
            Visibility="Collapsed">
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    x:Name="AccountUser"
                x:Uid="ShellPage_AccountUser"
                    FontSize="13"
                    FontWeight="SemiBold"
                    TextTrimming="CharacterEllipsis"
                    VerticalAlignment="Center" />

                <!-- 中间那个「·」：两行并成一行之后用它把名字和服务器隔开，墨跟着服务器那一行走（淡的一支）。 -->
                <TextBlock
                    x:Name="AccountDot"
                    Grid.Column="1"
                    FontSize="13"
                    Text="·"
                    VerticalAlignment="Center" />

                <!-- 服务器名走读数字（EgDataStyle）：和页头右上角那行、和卡片上的年份时长
                     是同一种字。它是机器串，不是一句话。 -->
                <TextBlock
                    x:Name="AccountServer"
                x:Uid="ShellPage_AccountServer"
                    Grid.Column="2"
                    Style="{StaticResource EgDataStyle}"
                    TextTrimming="CharacterEllipsis"
                    VerticalAlignment="Center" />

                <!-- 朝下：菜单从这颗按钮底下落出来（它在窗口顶上）。 -->
                <FontIcon
                    x:Name="AccountChevron"
                    Grid.Column="3"
                    FontSize="12"
                    Glyph="&#xE70D;"
                    VerticalAlignment="Center" />
            </Grid>
            <Button.Flyout>
                <MenuFlyout Placement="BottomEdgeAlignedRight">
                    <MenuFlyoutSubItem x:Name="ServerMenu"
                x:Uid="ShellPage_ServerMenu">
                        <MenuFlyoutSubItem.Icon>
                            <FontIcon Glyph="&#xE968;" />
                        </MenuFlyoutSubItem.Icon>
                    </MenuFlyoutSubItem>
                    <MenuFlyoutSubItem x:Name="UserMenu"
                x:Uid="ShellPage_UserMenu">
                        <MenuFlyoutSubItem.Icon>
                            <FontIcon Glyph="&#xE77B;" />
                        </MenuFlyoutSubItem.Icon>
                    </MenuFlyoutSubItem>
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem x:Uid="ShellPage_MenuFlyoutItem2" AutomationProperties.AutomationId="AccountSignOutItem" Click="OnSignOut">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xE7E8;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </Button.Flyout>
        </Button>
    </Grid>

        <!-- 页面那一块，从窗口最左边开始 —— 侧边栏删掉之后这里再没有第二列，所以从前那道竖发丝线（面板和
             片场之间那一根）跟着走了，框架给「工作区」画的那道边、那个圆角、那圈边距也一起没了（它们是
             NavigationView 模板的东西）。

             顶上留出 32（ChromeHeight，标题栏那一行）并自己上底色：外壳压在这一块的上面，页面从第 32 行
             起画，那 32 像素由这里补上底色（页面自己的底色和它同一支，多涂一层看不出来）。整套里只有主页
             把这 32 顶开 —— 见 HomePage.xaml 里那块大图。

             这一块整个收起（登录页、播放时）就是「浏览态在不在」，所以 Visibility 写在它身上：从前那是
             NavigationView 的活。 -->
        <Grid
            x:Name="ContentHost"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            Padding="0,32,0,0"
            Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- 左右 24，和 EgPageMargin 的左右 24 一样：面包屑和它底下那块场记板因此左对齐在同一
                 条竖线上。原来是 18，于是「媒体库 › 剧名」比页面标题往左出去 10 像素 —— 一整条别的
                 都对齐了的界面里，这种 10 像素是最显眼的那种脏。

                 底色和墨都由 PaintTitleInk 画，这里不写：详情页的剧照要「跟主页一样，占满标题栏」，
                 也就是从窗口顶边一路铺到这一行的底下，那时这一行必须是透明的 —— 一条实心条压在图上
                 就是那个要求要去掉的黑边。两种底两种墨，一处判断，写在代码里那一处。

                 ZIndex 而不是挪声明顺序：ContentFrame 是后一个孩子，天生画在这一行的上面，而页面里
                 那张固定的剧照正是往上顶过这一行的。数字提上来，字就在图上；顺序不动，读这个文件的人
                 还是先看到面包屑、后看到页面。 -->
            <Grid
                x:Name="TrailBar"
                Canvas.ZIndex="1"
                Padding="32,12,32,12"
                Visibility="Collapsed">
                <Grid.Resources>
                    <!-- 这一行的墨：字和字中间那个人字尖共用一支。
                         一支可改颜色的画刷、不是两个键各写一个色：改字典里的键对已经画出来的那几格
                         没有用（它们握着的是解析时那个对象），改这一个对象的 Color 才当场跟着变 ——
                         和 ThemeHost、和标题栏那两支墨同一个道理。
                         人字尖归框架的模板画，认的是 BreadcrumbBarNormalForegroundBrush 这个键，所以
                         构造时把同一支画刷也挂到那个键上（见 ShellPage.xaml.cs）。 -->
                    <SolidColorBrush x:Key="EgTrailInkBrush" />
                </Grid.Resources>

                <!-- ItemsSource is assigned in code: the trail is a private field, and a breadcrumb
                     of one entry only repeats the heading the page draws right below this row. -->
                <BreadcrumbBar
                    x:Name="Trail"
                    ItemClicked="OnBreadcrumbClicked"
                    VerticalAlignment="Center">
                    <!-- 走读数字：一条路径是「你在哪」的机器串，不是一句话，所以和页头右上角那行、
                         和账号下面那行服务器名同一种字。Binding 而不是 x:Bind —— 一格面包屑的数据
                         就是那个 Crumb 本身，靠它的 ToString 出字，没有属性可绑。
                         Foreground 写在这里而不是让 EgDataStyle 说：那支样式自己设了 Foreground，
                         而写在格子上的值压得过样式里的 setter。 -->
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate>
                            <TextBlock
                                Foreground="{StaticResource EgTrailInkBrush}"
                                Style="{StaticResource EgDataStyle}"
                                Text="{Binding}" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>
            </Grid>

            <!-- 每次导航由 BrowseTransition 选择入场动效；系统关闭动画时直接切换。 -->
            <Frame x:Name="ContentFrame" Grid.Row="1" />
        </Grid>

        <local:SignInPage x:Name="SignIn" />

        <!-- The player, a sibling rather than a page in the frame. It has to be able to take the whole
             window: the island is one shared surface, so anything still visible behind it would paint
             its own opaque background over the region where mpv's child window shows through. -->
        <local:PlayerPage x:Name="Player" Canvas.ZIndex="20" />

        <!-- The shell's one notification channel, replacing the WinForms toast window. Docked at the
             top because the player owns the bottom of the window, and closable because a report of what
             a playback did is worth reading at the user's pace rather than the timer's.
             顶上 44 = 标题栏那 32 加原来的 12：这一条不该压在那五颗按钮上。 -->
        <InfoBar
            x:Name="Toast"
            Canvas.ZIndex="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Margin="0,44,0,0"
            MaxWidth="560"
            CornerRadius="16"
            IsClosable="True"
            IsOpen="False" />
    </Grid>
</UserControl>
