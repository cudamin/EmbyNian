# EmbyNian 音视频输出补齐 · 任务书

对照主流播放器（PotPlayer / MPC-HC / mpv.net）清点 设置 → 视频输出、音频输出、着色器 三块之后，
真正缺的那几件。写于 2026-09-04。

## 0 怎么用这份文件

**这是增量，不是从零开始。先读工作树再动手。** 写这份文件时 `master` 在 `69f61bb`，工作区有五个文件
未提交（`AppSettings.cs`、`SettingsMigration.cs`、`ShaderGroupResolver.cs`、`SettingRow.cs`、
`SettingsViewModel.cs`），也就是说**另一个窗口正在改设置页和着色器判定**。所以：

1. 先 `git status` 和 `git diff`。下面每一条都要先确认它还没被做掉。
2. **行号只当线索，符号名才是定位手段。** 这份文件里的行号是 2026-09-04 12:50 那一刻的，而上面那两个
   文件当时正在被编辑，行号已经在漂。
3. 一条一条做，每条做完跑四道闸门（Release 构建 / 测试 / 发布 / `--self-check`），别攒着一起验。
4. 画面和声音好不好只有用户在屏幕前能判。**验证时不许真实播放**（会写进他真实的观看历史），
   要 A/B 就用 外部 `mpv.exe` 后端 + 本地文件。

## 1 已经核实过的现状（别重做）

清点结论：三块都在，视频和着色器基本齐全，**缺口集中在音频**。已经有的东西：

- **视频输出**：设置里 14 行（画质预设、`vo`、`gpu-api`、`hwdec`、色彩范围、视频同步、反交错、插值、
  高帧率/高刷回退、网络缓冲、抖动、去色带、HDR 处理、ICC），见 `SettingsViewModel.VideoCard`。
  播放器右键菜单另有一大片：宽高比、旋转、缩放平移、裁切填充、调色五项、去色带强度、HDR 六项、
  `hwdec` 切换、`vf` 滤镜十项 —— `Core/Mpv/PlayerMenuCatalog.cs:99`-193。
- **音频输出**：设置里只有 5 项（扬声器布局、动态范围压缩、独占模式、全局延迟、直通五个编码），
  见 `SettingsViewModel.AudioCard`。播放器菜单里另有声道输出方式、`af` 的 dynaudnorm/loudnorm、
  独占模式切换 —— `PlayerMenuCatalog.cs:195`-213。
- **着色器**：`ShaderGroupCatalog` 的 96 条链、色度重建进每一格、老片源独立轴、倍数按实际输出算
  加 0.05 回差、高帧率降链、前置选项从 `ShaderDescriptor.Requires` 推导、`NeutralOptions` 正反双向
  钉住、播放器菜单逐行写出每一档挂哪几个文件。**这一块功能上不缺**，见第 6 节。

**一个贯穿全篇的事实**：每次播放都是全新的 mpv 实例（`Playback/PlaybackService.cs:126`-175、
`Playback/LibMpvBackend.cs:48`、:64），启动时 `--no-config` / `config=no`
（`Mpv/MpvArgumentBuilder.cs:38`、`LibMpvBackend.cs:12`）。所以**右键菜单里改的任何东西都不存盘**，
换下一集全回默认；能跨片子活下来的只有设置文件里有字段的那些。下面好几条缺口都是这一条的推论。

## 2 第一批：音频（用户当天能听出来的）

### 2.1 音量上不去 100% 以上

mpv 自己的 `volume-max` 默认是 **130**，卡在 100 的是这个客户端自己。天花板写在**六处**，改一处不够：

| 位置 | 现在 |
|---|---|
| `Shell/Views/PlayerPage.xaml:567` | `Maximum="100"` |
| `Shell/ViewModels/PlayerViewModel.cs:1271` | `Math.Clamp(Math.Round(status.Volume), 0, 100)` |
| `PlayerViewModel.cs:1320` | `Math.Clamp(Math.Round(value), 0, 100)` |
| `PlayerViewModel.cs:1605` | `NudgeVolume` 的 `Math.Clamp(Volume + delta, 0, 100)` |
| `Core/Configuration/SettingsMigration.cs:151` | `Math.Clamp(settings.Audio.Volume, 0, 100)` |
| `Core/Mpv/MpvOutputOptions.cs:435` | `if (audio.Volume is >= 0 and < 100)` |

**最后一行是这条里唯一的陷阱**：那个 `< 100` 的本意是「100 就是 mpv 的默认，不必发」，但天花板一抬，
存着的 130 会因为这个条件**一次都发不出去**，mpv 每次从 100 开始，屏上滑杆停在 130 —— 又一个界面在
骗人。改成 `!= 100`（或者 `is >= 0 and != 100`）并补一条单测。

建议上限取 **130**（mpv 的默认 `volume-max`，不必额外下发任何选项）。要更高就得同时下发
`volume-max`，那时请一并想清楚超过 130 的软件放大是纯削波。上限这个数只写一处，六处引用它。

### 2.2 「动态范围压缩」这一行对大多数音轨不生效

`AudioCard` 那一行下发的是 `ad-lavc-ac3drc`（`MpvOutputOptions.cs:429`）。按 mpv 自己的说明，
那是 **AC-3 解码器**的选项，而且要片源自带 DRC 元数据才有效 —— DTS / TrueHD / AAC / FLAC 轨一律没反应。
设置页那一行的说明只写了「mpv：ad-lavc-ac3drc」，没有这句话。**这就是这个项目最在意的那类「界面在骗人」**
（和 画质预设写 `scale` 却被链盖掉、视频同步显示「不指定」而实际 `display-resample` 是同一类）。

真正跨编码的答案已经在仓库里，但只活在右键菜单、而且不存盘 —— `PlayerMenuCatalog.cs:206`-210 那个
「切换 下混滤镜」，`dynaudnorm` 和 `loudnorm` 两档。用户的输出链路是**电脑扬声器或耳机**，所以
「对白清不清楚」对他是个常设偏好，不是每部片子现调的东西。

要做两件：

1. **那一行的说明补上适用范围**，照现在的行文风格写清「只对 AC-3 / E-AC-3 轨有效，且要片源带 DRC 信息；
   其它编码请用下面那一项」。别改选项本身 —— 它对 AC-3 是真有用的。
2. **新增一行「音量均衡」，四档**：不启用 / `dynaudnorm`（连续跟随，适合夜里看）/ `loudnorm`（对齐到固定
   响度，适合一集接一集）/ 保留原样。值就用菜单里那两条现成的滤镜串，**滤镜串只写一处**，菜单和设置页
   共用；写进 `AudioSettings` 一个新字段，由 `MpvOutputOptions.Build` 下发成 `af`。
   注意 `af` 是个列表选项，2.3 那条如果也落在 `af` 上，两者要能共存而不是互相覆盖。

### 2.3 5.1 下混到两声道没有归一化开关

两声道听 5.1 片的老问题：对白太小声，爆炸太大声。mpv 有 `audio-normalize-downmix`，**默认 no**，
仓库里一处没设过。

**不要默认打开**，做成一行带说明的开关：开了整体会更轻（这是上游自己写的代价），而且它只在**下混是
mpv 做的**时候才起作用 —— libavcodec 自己下混的那条路要看 `ad-lavc-downmix`。所以这一条落地前请先在
外部 `mpv.exe` 后端上拿一个真 5.1 本地文件量一次：`ad-lavc-downmix` 是什么值、`af` 链上到底有没有它。
量不出来就先只做开关加说明，别在 C# 里替 mpv 猜。

## 3 第二批：截图

**全仓库没有 `screenshot`。** 这是三块里唯一一个「主流播放器人人都有、这儿完全没有」的视频功能。

不做的理由写在 `PlayerMenuCatalog.cs:70`-74：`--no-config` 之下没有 `screenshot-directory`，
文件会落到 exe 旁边而不告诉用户。**这个理由现在不成立了** —— `Infrastructure/AppPaths` 已经在管日志和
缓存目录，`SettingsViewModel.AboutCard` 里那三行还各带一颗「打开文件夹」按钮。

做法：基线里下发 `screenshot-directory`（AppPaths 下新开一个 `screenshots`）、`screenshot-format=png`、
`screenshot-template`（片名 + 时间码，别用 mpv 默认的 `mpv-shotNNNN`），然后把菜单那三行装回去
（`screenshot`、`screenshot video`、`screenshot window` 各自的区别要在标签里写清：带不带字幕、带不带
着色器）。截完 `show-text` 报出落在哪儿，关于卡照 AboutCard 的样子加一行读数加一颗按钮。

`screenshot-directory` 归 `MpvBaseline`（它就是「从前由 mpv.conf 提供、现在必须由客户端明说」的那批），
不是设置项 —— 除非用户要挑目录，那再加一行 `PathBox`。

## 4 第三批：音频输出设备选择

**设置、播放器、诊断三处都没有 `audio-device`。** 唯一提到它的是一段解释为什么砍掉的注释
（`PlayerMenuCatalog.cs:61`：uosc 那一行要异步读属性才能画勾）。主流播放器人人都有这一项。

它现在的实际代价：**音频独占模式只能占「系统默认设备」**。插上耳机之后独占的是哪一个，取决于 Windows
那一刻认哪个是默认 —— 用户看不到，也没得选。

难点在设置窗口手上没有 mpv 实例，而设备列表要从 mpv 读（`audio-device-list` 属性）。三条路，
**推荐第一条**：

1. **临时开一个 libmpv 句柄只为枚举**：`mpv_create` → 读 `audio-device-list` → 销毁，不建窗口、不放任何
   东西。这正是 `mpv --audio-device=help` 自己做的事，也是唯一能把真实设备名（`wasapi/{GUID}` 那种）
   拿准的办法。放 Core，接口收窄成「给我一份设备清单」，设置页只消费它。
2. 播放器菜单里做 —— 那儿有活的 mpv，但打开菜单会变成异步，`PlayerMenuCatalog` 是静态数据、刻意不异步。
3. 让用户填设备名 —— 不要。那串 GUID 没人抄得对。

落地要注意：设备列表是**会变的**（插拔耳机），所以存进设置的那个值要能「认不出来就回落到默认设备」，
按 `Pick` 那一手（`SettingsViewModel.cs` 的 `Pick`，认不出的值单独成一项标「设置文件中的值」）处理，
别让一个拔掉的耳机把播放弄成没声音。诊断页顺带加一行「本次播放实际用的输出设备」。

## 5 小件

- **音轨语言只能选一个**（`PlaybackSettings.AudioLanguage` + `AudioTrackMode`，`AudioLanguages` 下拉），
  而字幕那边是优先级列表（`SubtitleLanguages`，`Languages()` 那个行工厂）。mpv 的 `alang` 本来就吃列表。
  想要「日语 > 粤语 > 英语」现在表达不出来。改成和字幕同一种行（同一个解析器 `TrackLanguagePriority.ParseList`），
  单语言是它的退化情形，迁移把旧的单值读成一元列表。
- **`Emby/PlaybackReport.cs:30` 的 `VolumeLevel` 声明了但全仓库没有赋值**，Emby 那边的遥控看不到音量。
  要么填上（`FlushVolume` 那条路上已经有这个数），要么删掉字段 —— 留着一个永远是默认值的上报字段是纯误导。
- **画面几何不持久**：宽高比 / 裁切填充 / 旋转 / 调色在右键菜单里改完，下一集全回默认（理由见第 1 节末）。
  主流播放器会记住。**优先级低**，因为 锁定窗口比例大小 已经把常见情形挡掉了；真值得记的只有 2.35:1 片子的
  裁切填充。要做就只做这一项，做成设置里一行「宽于 16:9 的片源默认裁切填充」，别把六个 mpv 属性全搬进设置。

## 6 明确不做的，以及将来什么条件下再做

- **刷新率自动切换（24p 输出）。** 主流有（MPC-HC / PotPlayer / Kodi），mpv 自己做不到，要调 Windows 显示
  模式 API。**但在这台机器上是真的不需要**：144 ÷ 24 = 6.000 整除，没有节拍要补，这一点已经实测过并写在
  `MpvOutputOptions.cs:311`-343。换了 60Hz 的屏或投影仪再说。
- **均衡器。** PotPlayer 有。对一个看片客户端价值很低，而且 `af` 上已经要放 2.2 和 2.3 两件东西了。
- **耳机虚拟环绕（HRTF）。** 要 SOFA 文件，等于给程序装一份来历和许可都要交代的资产。**再做的条件**：
  用户明确要，而且找到许可清楚的 SOFA。
- **着色器强度可调。** 不是没做，是做不到：`adaptive-sharpen` 的强度是编译期 `#define`
  （`assets/shaders/README.md:28`-30），mpv 没有对应的运行时选项。上游哪天给了 `//!PARAM` 再说。
- **自己加载 .glsl / 自己组一条链存下来。** 这是着色器这块唯一一个主流（MPC-HC 的着色器编辑器）有而这儿
  没有的功能。按现在的设计八格是代码数据（`ShaderGroupCatalog`），所以这是**一个功能需求，不是改一改**：
  要有一份用户着色器目录、一个链编辑界面、一套「用户链也要过三条互斥校验」的规则。**别顺手做半个。**
- **显卡档自动检测。** 条件已经写在 `画质档位重构-任务书.md` 第 6 节，照那条走（持续掉帧降一档），
  不要引入型号表。

## 7 测试清单

- **音量天花板六处一致**：上限那个常量改一个数，六处跟着走；存 130 时 `MpvOutputOptions.Build` 真的发出
  `volume=130`（这是 2.1 那个陷阱的回归测试）；存 100 时不发；存 0 时发 0 而不是被当成「没设」。
- **`af` 两件事共存**：只开音量均衡、只开下混归一化、两个都开、都关 —— 四种组合下 `Build` 出来的 `af`
  都是合法的一条链，且互不覆盖。
- **动态范围压缩那一行的说明**里出现「AC-3」字样（防止哪天又被「整理」掉）。
- **音频设备**：认不出来的设备名回落到默认而不是无声；设备列表为空时那一行仍然可用。
- **截图**：`screenshot-directory` 一定被下发（不下发就是文件落到 exe 旁边的老问题）；模板不含
  非法文件名字符。
- **音轨语言列表**：单值旧设置读成一元列表；`日语 > 粤语` 这种写法解析成两项而不是一项
  （字幕那边栽过这个，见 `Languages()` 的注释）。

## 8 验收

每一条各自四道闸门全绿，然后这几件要用户在屏幕前判：

1. 一部安静的片子，音量拉到 130，是不是真的更响（而不是滑杆动了声音没动）。
2. 一部 5.1 的片子，音量均衡 三档各听一段对白 —— 哪一档是他要的。
3. 同一部 5.1 片子，下混归一化 开关各听一段 —— 是不是「不炸了但整体变轻」，值不值。
4. 插上耳机之后，音频设备那一行选到耳机、独占模式开着，声音在不在耳机上。
