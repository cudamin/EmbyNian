# EmbyNian — Claude 常设规则

本文件是当前开发规则的权威出处；命令与操作细节见 [docs/开发与验证.md](docs/开发与验证.md)，项目技能保存专项方法，源码与脚本是版本、参数和能力的事实来源。[PROGRESS.md](PROGRESS.md) 记录在途工作与历史证据，按任务需要读取；历史记录不替代现行规则。

## 首要目标

- **技术规则是手段，不是目的。** 分层、MVVM、DI 和接口是默认选择；当它们让代码更绕、更容易出错时，采用更简单的路径，说明位置、原因与取舍，并在 PROGRESS.md 记一行。
- **用户的技术指示可以讨论。** 若点名的实现方式会让代码变差，用一两句平实的话说明代价；用户再次确认后照做。
- **简单不等于行数少。** 看改一个行为需要打开多少文件、单个文件能否读懂、重要决定能否用测试验证。保留解释约束与设计原因的注释，不为缩短代码删掉维护者需要的上下文。
- **用户决定产品范围和可见体验，助手负责实现与验证。** 不把“需要用户看一眼”当作省略可执行检查的理由，也不以测试通过代替视觉检查。
- **简化不能豁免流程与安全。** 验证与交付要求、真实媒体库保护、凭据和 Git 规则仍须遵守。机器相关说明是带适用条件的经验；出现不同证据时先核实，不把旧故障当作永久诊断。

## 项目与技能

Emby 桌面客户端：WinUI 3、Windows App SDK、.NET 10、C#，非打包、x64，播放内核 libmpv。SDK 版本见 `global.json`，包版本见各项目的 csproj。

- `src/EmbyNian.Core`：不依赖 UI 框架。目前不引用 NuGet 包，这是保持轻量的选择，不是“有包就不能测试”的限制。
- `src/EmbyNian.Shell`：WinUI 外壳。
- `tests/EmbyNian.Tests`：目前只引用 Core 的离线控制台测试运行器。需要覆盖其他无 UI 逻辑时可以调整测试边界，不必为了现有引用把所有判断搬进 Core。

**播放管线与后端是两件事。** 集成管线将 mpv 的 Composition 交换链嵌入 WinUI 视觉树；独占管线（Standalone，亦称独立模式）使用 mpv 自建的顶层窗口，控件来自 `assets/mpv-ui` 的 uosc，同窗换片兼容性由 `src/EmbyNian.Core/Mpv/InlineSwitch.cs` 判定。默认管线以 `AppSettings.cs` 为准；内置 libmpv 与外部 mpv 进程后端的细节见播放技能。

按任务读取随代码入库的技能：

- [embynian-winui-shell](.claude/skills/embynian-winui-shell/SKILL.md)：修改或审查 C# / XAML 前读取，说明分层、DI、Attach 与 UI 陷阱。
- [embynian-playback](.claude/skills/embynian-playback/SKILL.md)：播放后端、进度上报、字幕音轨、换片，以及 `assets/mpv-ui` 中的 Lua/uosc 独占控件。
- [embynian-verification](.claude/skills/embynian-verification/SKILL.md)：验证读数、截图、指针移动及其证据。
- [mpv-shader-quality](.claude/skills/mpv-shader-quality/SKILL.md)：着色器档位与画质链。

通用 `winui-*` 技能来自用户级技能目录或插件。**与本文件冲突时以本文件为准。** 规则改动须同步相关技能、开发文档、脚本帮助与运行提示；不在技能里另立升级、验证或安全政策，不在 `.workbuddy/`、`.zcode/` 复制规则。当前文件数、测试数、控件数从本次源码或工具输出获取。

## 分层与代码风格

以下是实现默认值，服从首要目标：

- **纯 UI 行为可以留在 code-behind。** 显隐、Frame 导航和返回栈、菜单项构建、窗口按钮及需要知道事件来源的行为，不必为 MVVM 增加中间层。
- **业务与外部交互经 Service。** HTTP/Emby 调用、持久化和播放生命周期不放进页面；视图模型从服务取得能力，普通 MVVM 管道使用 CommunityToolkit.Mvvm。`Views/ItemCommands.cs` 是现有旧债，不作为新增代码模板。
- 无 UI 依赖的业务能力优先住 Core；需要共享依赖或生命周期的服务注册于 `src/EmbyNian.Shell/Composition/ShellServices.cs`，然后注入。纯函数、模型和无依赖的小工具不需要为“进容器”包装成服务。
- **重要业务规则、容易回归的算法和可复用的计算优先成为 Core 中的命名纯函数，并用测试验证。** 局部展示判断就近保留；续播、选集、设置迁移等业务语义不能以“只是一个判断”为由逃避测试。
- 不因为类进了 DI 就加接口。接口用于替换、隔离或收窄能力；`ISettingsService`、`IServerCapabilities` 等收窄访问范围的接口不要机械地换成具体类。
- 页面默认自建视图模型，通过 `Attach(...)` 接收依赖，并守卫尚未附加的调用。`PlayerViewModel` 由容器持有，因为播放状态需要跨页面导航存活。
- 复用现有类型；只有确实改善职责或复用时才抽新类，不按 `Manager` / `Helper` / `Factory` 的名字判断设计好坏。`View → ViewModel → Service → 外部世界` 是方向，不是每个文件都必须走完的链。
- **绑定到 `ItemsSource` 的集合对象保持稳定**：默认 `{ get; }` 加初始化器，允许原地增加、删除、移动和更新。需要整体刷新时才 `Clear()` 后重填；小改动避免全量重建，并注意选择与滚动状态。

格式以 [`.editorconfig`](.editorconfig) 为准：C# 文件范围命名空间、私有字段 `_camelCase`、const / static readonly 用 `PascalCase`、不用 `this.`、4 空格；文本使用 LF，PowerShell 使用 UTF-8 带 BOM。命名等 `IDE0###` 尚未作为构建门禁，空白检查通过不等于全部风格通过。

## 项目约定与自动检查

`tests/EmbyNian.Tests/AgreementsTests.cs` 与同目录 `Agreements.txt` 记录约定。**基线是要求复核的提醒，不是禁止合法重构。** 新增、改名、合并或退役检查时，先核对调用者、自动化脚本和相关文档，说明旧检查为何不再适用、保留什么覆盖，再逐行更新基线并运行受影响检查；不为让测试变绿整份接受新快照。仅因检查难跑或出现失败，不构成退役理由。

- **嵌套绑定**：Shell csproj 对 `WUI2010` 的压制有明确前提，原因与失效条件写在 `NoWarn` 旁。新增三段绑定路径时先读那里；`bind` 基线提醒复核。其他分析器警告也要调查，不能无依据消音。
- **自动化定位**：没有显式 AutomationId 时，WinUI 会把 `x:Name` 作为句柄；已有它就不重复添加同值属性，`x:Uid` 不算句柄。普通交互控件应可稳定定位。模板与复用控件允许“所属列表项 + 局部句柄”，不要求全局唯一。新增交互控件后运行 `tools/scan-automation-ids.js`；脚本跳过模板与部分复用控件，这些范围仍需检查运行中的 UIA 树。`handle` 基线防止已有句柄无意消失，改名须同步使用者。
- **字号与颜色**：文本使用 `Theme/Styles.xaml` 的项目刻度与 `Eg*Style`，主题颜色使用角色键；`FontIcon` 的数字是字形度量，按实际需要处理。`font-size` / `color` 基线逐文件记录内联数字和颜色，只减不增；必要例外或文件搬迁需复核后更新，计数减少不证明视觉正确。
- **命令行**：`switch` 基线检查源码中的开关集合；变更时同步 [开发文档中的开关说明](docs/开发与验证.md#命令行开关)，并验证组合与实际用法。
- **分析器载荷**：构建使用仓库 `tools/analyzers` 的副本，`analyzer` 基线核对文件与哈希。`Directory.Build.props` 的 `Condition=Exists` 会让缺载荷的构建继续，所以“0 警告”与载荷基线检查都必须通过。

## 依赖与分析器升级

**主动跟进稳定版，但作为独立的一轮改动，不顺手混入无关修复。** 不采用 preview / rc / beta。先说明范围，改版本后还原依赖，再走完整四道闸门；运行覆盖不到的部分照实报告。

- NuGet 版本来自项目文件；改包版本或新工作树首次构建前完成 restore，不能拿旧 `obj/project.assets.json` 验新版本。
- WindowsAppSDK Runtime 升级要关注 onnxruntime 等新增载荷；报告实际包体积变化，由用户决定是否接受明显增重。
- `libmpv-2.dll` 是手动维护的原生二进制；更新后验证构建、发布与受影响的管线，仍遵守播放安全边界。
- **发版使用仓库已确定的分析器与哈希。** `tools/refresh-analyzers.ps1 -Check` 只供发现本机插件差异，不是发版必过项；按写时间选候选不保证版本最新，找不到插件时的退出码 0 也不代表载荷完整。
- 升级分析器须明确来源（必要时传 `-SkillRoot`），复核新诊断与哈希后逐行更新基线并完整验证，不为迁就本机旧插件降级仓库载荷。

## 四道闸门与日常交付

**执行命令见 [开发文档「标准命令」](docs/开发与验证.md#标准命令)。** 所有命令在当前工作树根目录运行；本机用 `$HOME/.dotnet/dotnet.exe`，按 `global.json` 核实，不用 PATH 上的旧 SDK。构建保持 Release、单节点及文档所列 MSBuild 标志，发布脚本也遵守同样约束。

| 改动类型 | 必须完成的验证 |
| --- | --- |
| 纯文档 | 内容、链接、命令与差异一致性；不构建、不重发应用 |
| 日常代码、脚本或应用资源 | 先编译当前测试项目及 Core，再跑闸门 2；收尾跑闸门 3 |
| UI 布局、字号、交互 | 日常检查，加受影响页面、相关窗口尺寸的运行检查和截图；新增控件检查自动化定位 |
| 主题颜色 | UI 检查，加每个存活主题各一张真实屏幕截图 |
| 播放相关 | 日常检查，加下方三档范围内适用的单测、隔离探针；分开报告后端与管线覆盖 |
| 验证工具 | 日常检查，加实际运行受影响的检查路径及失败场景；改自检必须当轮跑它 |
| 发新版本、升级依赖或分析器 | 闸门 1～4 全跑，包括空白检查和分析器哈希检查 |

多种改动取要求的并集，不用“只是文档/资源”掩盖同树待交付的产品改动。中间步骤可以运行本树开发构建，不必反复发布。

1. **完整构建与空白检查。** 构建须 0 警告、0 错误；`dotnet format whitespace` 须通过。Git 行尾归一化可能隐藏 CRLF 漂移，不能用 diff 干净替代；PowerShell 编码/语法另跑脚本检查。
2. **先编译，再测试。** 编译当前 Release 测试项目及 Core 成功后才用 `--no-build`；完整方案已成功编译当前代码且此后未改相关代码时可复用输出。编译失败立即停，不跑旧测试冒充验证。测试须退出码 0、失败 0，跳过项单独报告；发布不能补上测试编译。
3. **发布到实际交付目录。** 收尾更新桌面快捷方式指向的 `artifacts/publish/win-x64`，只 build 不 publish 不算交付。发布前确认目标程序未开着；文件锁须请用户关闭，不擅自杀进程。未刷新须说明“快捷方式仍是旧版”。独立工作树的默认发布只更新该树；由一个交付会话整合、重验并发布，其他会话不覆盖共同目录或改快捷方式。`-OutputRoot` / `-SkipPublish` 成功不等于已交付。
4. **自检与基线比较。** 用 `tools/selfcheck-diff.ps1` 判定本次日志：不能有失败、未经确认的检查消失或“通过 → 信息/跳过”降级。`-Log` 只比已有日志，不证明当前构建已运行。更新 [基线](docs/selfcheck-baseline.txt) 须遵守上方新增/改名/退役流程，不能接受失败来洗绿。已知旧失败也要如实报红，历史记录中的“可接受”不豁免现行闸门。

**运行目录也是隔离边界。** 自检只读已保存设置，在独立运行目录使用副本、写日志与缓存，禁止改后再整份恢复真实设置。工作树隔离不等于用户数据或桌面隔离；使用同一桌面/用户数据的 UI 验证须串行，不能覆盖另一轮证据。普通自检仍可能登录保存的账号、读取真实库，不能称为离线或据此证明真实播放。

自检默认副屏，单屏自动回退；`--screen 1` 可指定主屏。颜色截图从 `UiThemes.cs` 读取存活主题，当前均为深色；不要恢复已删除的主题。浅色派生逻辑的保留理由见该文件注释。截图与指针证据按验证技能执行。

## 播放相关改动的验证（三档）

内网 Emby 服务器是用户的真实媒体库，不是测试夹具；地址与端口不写进公开仓库，真实播放会改变观看历史和续播点。

1. **默认不播放。** 用 `--show-detail` / `--show-episode` / `--show-library` / `--show-osd` / `--show-cover` 展示界面，配合主题与截图。普通导航可能联网读取真实库，不把“不播放”说成“不联网”。
2. **播放管线改动使用隔离本地探针。** `--probe-cursor`、`--probe-player-motion`、`--probe-composition` 使用独立数据目录，不登录、不启动 Emby 播放、不上报记录；可选本地素材拒绝 URL、网络盘、重解析路径和外部网络引用。**三条目前固定走集成管线**，不覆盖独占窗口、uosc、独占同窗换片或外部 mpv 后端；相关改动须报告单测与运行覆盖缺口。新增离线入口保持同样边界。
3. **真实服务器播放或有副作用的验证先获授权。** 按用户当次确认，或其明确授权的专用测试账号及操作范围执行。测试账号应限制权限，只隔离观看记录，不隔离服务器负载、共享媒体和元数据；建立账号、删除媒体、修改共享数据均不能自动获授权。

各档共同遵守：

- **不点卡片中央或海报本体**，那里会浮出真实播放按钮；用导航开关、hero“详情”、面包屑或底部标题条，不依赖 Escape 撤销误起播。
- 用命令行开关导航，不用坐标点击。用户可能使用同一只鼠标；写任何移动指针的验证前读取验证技能的 “Moving the pointer, and proving it moved”，并验证目标确实收到输入。权限拒绝不换工具绕过。
- 悬停浮层提供 `--show-*` 状态入口，不靠停住指针截图。`--show-menu`、`--show-osd`、`--hide-cursor` 各自单独用，互不混用、不与 `--self-check` 混用；探针各自单独运行。
- `--play`、UIA invoke 播放按钮等都是真起播，同受第三档约束；工具名称不改变副作用。

## 凭据

Emby 令牌用 DPAPI 加密存储，**不打印、不写日志/自检报告，不进入 URL、助手命令或应用启动子进程的命令行参数**。请求头经进程内 API 或经验证的进程间通道传递；请求头不在 URL、日志已脱敏，都不能证明启动参数安全。外部播放器不自动获得例外。

内嵌 Emby 网页控制台为兼容其客户端，只在受信任服务器来源的 `localStorage` 传递凭据，不用查询字符串、不向其他站点注入。报告中的令牌出现次数必须为 0；验证使用占位令牌，不读取真实明文来做断言。

## Git 与协作

- `origin` 是 `https://github.com/cudamin/EmbyNian.git`；凭据由本机 Git Credential Manager 保管，不进命令、脚本或远程 URL。
- 长期主干是 `master`，不维护第二条跟随主干的长活分支。隔离工作可用临时分支，不假定任意工作树的 HEAD 在 master。主干交付推 `git push origin master`，不做 `git push . <branch>:<branch>` 的 ref 搬运。
- **同树同时只有一个写入会话。** 先检查状态，保护已有未提交工作；并行写入使用独立 `git worktree`，在各自 PROGRESS 记树和文件范围。构建、测试和临时产物各自在本树；运行验证与交付按上方规则串行。
- **命令定位当前工作树。** 优先绝对文件路径，根目录须与 `git rev-parse --show-toplevel` 一致；不依赖上一轮 shell 的 `cd`，不固定跳回主目录。
- 不 reset，不用 checkout / restore 覆盖工作区。撤销自己的实验也要先核对 diff，再定点手改，不丢掉用户或其他任务的内容。
- 只有用户明确要求才提交。中文提交正文加 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`；提交后立即推 origin，不必再问。未获授权不提交、不上传。源文件使用 LF。

## 本机工具与故障排查

具体经验和适用条件见 [开发文档](docs/开发与验证.md#本机工具与故障排查)。使用用户级 SDK 和 `py` 启动器；PowerShell 保持 UTF-8 带 BOM、LF。先验证当前错误的证据，不用通用 setup、清空输出或反复发布来试运气。真实屏幕截图与不合成 Mica/鼠标的渲染截图不能互相冒充。

## 汇报

用中文、大白话讲结果，不贴代码。说明改了什么、实际验证了什么、哪些没有验证，以及用户需要知道的代价。实现细节自行决定；只把范围、优先级、可见行为、不可逆操作和规则取舍交给用户，不提供无关技术选项菜单。
