# EmbyNian 画质档位重构 · 任务书

2026-09-03 定稿。**取代此前那份《着色器与画质档位方案-修订版》**，那份里被改动的地方见第八节。

## 0 怎么用这份文件

**这份文件到达的时候，这活已经在做了。** 2026-09-03 的工作树里已经有：`assets/shaders/` 二十一个入库的着色器加一份出处说明、新文件 `Core/Mpv/UpscaleTier.cs` 和 `ShaderGroup.cs`、以及对 `ShaderGroupResolver`、`PlaybackPlanner`、`MpvOutputOptions`、`HostWindow`、csproj、`publish.ps1` 和两份测试的改动，全部未提交。所以**这份是增量，不是从零开始**：先读工作树，已经做到的地方别推倒重来，只按下面各节纠正还不对的部分。第八节列了哪些结论变过。

- **分两批交付。** 第一批做完、四道闸门全绿、用户在屏幕上看过之后，再动第二批。第二批的东西不要提前混进第一批。
- **规则优先级**：`CLAUDE.md` 的首要目标 > 本文 > 旧代码现有行为。本文和旧行为冲突时以本文为准；本文某一条让代码变得更长更绕时，按首要目标办 —— 换简单写法，并在 `PROGRESS.md` 记一行：改了哪条、为什么、换成了什么。
- **每条规则本文只说一遍。** 上一版同一套约束写了四遍（原则、契约、勾选框、正文），已经开始互相矛盾。要改就改这一处，不要另起一段重申。
- **用户已定的四条，不许自行反转**（下面各节都是按这四条写的）：
  1. 放大倍数按**实际渲染输出尺寸**算；开播时同时算窗口和全屏两套方案。
  2. 显卡档**默认低档**，保留手选中/高档。**这一轮不做自动检测。**
  3. `ravu-zoom-ar-r2` 那 2.9 MB 留着。体积在这个项目里不是约束（发布目录 298 MB，`libmpv-2.dll` 一个 117 MB）。
  4. 低档那一列的原则是**保持现状 + 补缺**。会改画风的改动一律做成可选项，交给用户 A/B，不动默认。

## 1 出发点：核实过的事实

2026-09-03 逐文件读过 + 联网核实。上游会变，动手前重查一遍。

- **`igv/FSRCNNX_x1_16-0-4-1_distort.glsl`（230 KB）根本不放大。** 全文没有一条 `//!WIDTH` / `//!HEIGHT` / `//!WHEN`，`x1` 就是一倍，只在片源分辨率上做增强。所以 `2K-iGPU-SD` 组注释里那句「低清 2–3 倍放大，算力花在 FSRCNNX 上」是错的 —— 放大全是 `scale=ewa_lanczossharp` 干的。这是低清那一档最该修的地方。
- **`Ani4K/Ani4Kv2_ArtCNN_C4F32_i2.glsl`（785 KB）是转手的 ArtCNN**：五十个 pass 都带 1.2 倍门控，最后一个 pass 输出两倍，本质是个带门控的 2 倍亮度放大器。文件头两个许可，训练权重是 CC BY-NC 4.0。上游 `ArtCNN_C4F32.glsl`（743 KB）是同一个东西的原版，只有 MIT，且在维护。
- **五个内置组（`2K-iGPU`、`-Anime`、`-Light`、`-Anime+`、`-SD`）是真为「Ryzen 5600G + Vega 核显 + 1440p 屏」调的**，除 FSRCNNX 那一处外设计站得住。别当抄来的清掉。
- **Anime4K 最后一版停在 2021-10-26**；igv（SSimDownscaler / SSimSuperRes / KrigBilateral / adaptive-sharpen）和 agyild（FSR / CAS / NVScaler / SGSR）两批 gist 2026-08 还在改；**ArtCNN 的 `Chroma` 系列只有 ONNX、没有 GLSL**，所以色度这块只能走 CfL。
- 代码里三处缺口：**决策只吃片源宽高、从不看输出**（`ShaderAutomationSettings.Resolve(looksAnimated, sourceWidth, sourceHeight)`）；**画质预设和配置组各写一套 `scale`/`cscale`/`dscale`**，三层拼接里组在最后所以组总赢 —— 设置页显示「画质预设 = HQ」时生效的其实是组，界面在骗人；**五个组一个色度着色器都没有**，只有 `cscale=spline36`，而 4:2:0 的色度横竖都只有亮度的一半。

## 2 判据

### 2.1 放大倍数与五档

倍数 = **实际输出高 ÷ 片源高**。同时算宽度比；等比片源以高度为准，两者相差超过 5%（非等比/变形片源）时按较大的那个定档，并记一条日志。

```
< 1.05              Downscale       缩小（≥0.95 时 OSD 上写「原生」）
1.05 – < 1.45       MicroUpscale    微放大
1.45 – ≤ 2.20       SweetSpot       甜点
> 2.20              LargeUpscale    大倍数
```

四个区间互斥、覆盖全部有效正数倍率。

**我原先加的第五档 `Native` 撤掉了。** 读了 `assets/shaders/README.md` 之后发现它多余：`SSimDownscaler` 自己带门控，只在真的要缩小时才跑，所以 1.00 倍时它挂在链里一分钱不花。剩下的只是 OSD 上「1.00× · 缩小档」这个标签别扭 —— 倍数 ≥0.95 时把标签写成「原生」就够，不必为一个标签单开一档加一行表。

**这批着色器有几个自带 `//!WHEN` 门控，选档时必须知道**（细节在那份 README 里）：`ravu-lite` 输出不到 1.2 倍不跑，`ArtCNN` 不到 1.3 倍不跑，`ravu-zoom` 只要在放大就跑并直接输出到目标尺寸，`SSimDownscaler` / `SSimSuperRes` 各自只在缩小 / 放大时跑。**后果**：微放大档（1.05–1.45）里唯一每一格都会出手的放大器是 `ravu-zoom`；这一档如果挂 `ArtCNN`，1.05–1.30 那一段它是不出手的，由 `scale=ewa_lanczossharp` 收尾 —— 那一段本来收益就小，可以接受，但要知道是这么回事，别以为链里写了就一定在跑。**这也是不许靠文件名判断一个着色器干什么的又一个理由。**


**边界要有 0.05 回差。** 判档的纯函数吃「当前档位」作为可选入参：已经在某一档里时，把该档上下各放宽 0.05 再判，仍落在放宽后的区间就保持不变。没有回差，慢慢拖窗口经过 1.45 会让放大器在 `ravu-zoom` 和 `ravu-lite` 之间来回换，每次换都是一次画面锐度当场变化加一次着色器重新加载。

### 2.2 老片源是独立的一个轴，不要塞进倍数档

上一版把「先降噪去带再放大」写进大倍数档，这是把两件事混成一个轴：噪点和色带取决于**片源有多烂**，不取决于**要放多大**。后果能算出来 —— NTSC 的 DVD（480 线）放到 1080p 是 2.25 倍，落大倍数档，有去带；PAL 的 DVD（576 线）放到 1080p 是 1.88 倍，落甜点档，一条去带都没有。同一张碟不同区，处理完全不同。

所以：**片源高度 ≤ 576 → 老片源**，与倍数档并行判断，是另一个纯函数。老片源加 `hdeband`（低档也加，6.5 KB、很便宜）；`nlmeans_light` 只在中/高档加。用户可以手动关掉。

### 2.3 输出尺寸从哪来

优先级：**当前实际渲染目标尺寸 → 最近一次成功取到的有效尺寸 → 所在显示器尺寸**（仅兜底，并记一条日志）。取不到时**不许假定 4K**，也**不许永久锁死显示器原生分辨率**。

动手前先确认每个播放后端各自怎么拿这个尺寸（**包括外部 `mpv.exe` 那个后端**）。拿不到的后端就走兜底并在日志里标明，不要为这件事改后端结构。

### 2.4 开播算两套，全屏切换换预案

开播 / 切片时算两套方案：**当前窗口尺寸**一套，**所在显示器全屏尺寸**一套。

- 进 / 退全屏：直接换成预备好的那一套，不重新判定、不等防抖。
- 窗口尺寸变化：记下待定尺寸，稳定 **400 毫秒**后算倍数，按 2.1 的回差判是否跨档。不跨档就只更新上下文里的输出尺寸（`ravu-zoom` 这类按目标尺寸渲染的着色器要用新尺寸），跨档才重新生成链。
- 窗口被拖到另一台显示器：和全屏一样按离散事件处理，两套方案都重算。
- 拖动过程中一次都不许重新生成链。防抖时长放统一的策略常量里，不要散在业务代码。

**代价说清**：预案省下的是「判定」，不是「编译」。全屏前后如果跨了档，切全屏时 mpv 仍要重新加载着色器，会有一次很短的顿。这是可以接受的 —— 它是用户按键触发的离散动作；反过来（小窗口里跑全屏规格的链）是持续掉帧。

## 3 第一批：用户当天能看见的部分

顺序就是下面的顺序，每一步做完都应该是一个可验证的状态。

**3.0 先把 CfL 和亮度放大器的先后关系实测掉。** 表里每一格都依赖这个结论，它必须排在最前面，不能留到最后。做法：用**外部 `mpv.exe`** 加一段**本地视频文件**（不要走 Emby 会话，会写进真实观看历史），暂停后读 mpv 的性能统计页 —— pass 列表会列出每个 pass 及其输出尺寸，直接看得出 CfL 在哪一步、它读到的 luma 是几线；再用 `screenshot window` 截「开 CfL / 关 CfL」各一张对比。至少确认四件：亮度在哪一步改变尺寸、CfL 读到的是哪个尺寸的亮度、CfL 在当前链里能否安全执行、渲染层实际执行顺序是否和设计一致。**结论写进代码里的表和一个单测钉住，不要只写成注释。**

**3.1 着色器入库 —— 已经做完了**（2026-09-03，未提交）。二十一个文件在 `assets/shaders/`，出处、许可、唯一一处本地改动（`igv/adaptive-sharpen.glsl` 的 `curve_height` 从上游 1.0 改成 0.5，更新这个文件时要重新改回去）、以及哪几个文件自带门控，全部记在 `assets/shaders/README.md`。**那份 README 是这批文件的唯一出处说明，别在别处再抄一遍。** 这一步还剩的只有：csproj 的拷贝清单和 C# 档位表引用同一批文件名，由现有那个「两处不许漂移」的单测钉着。

**3.2 倍数与档位的纯函数进 Core。** 吃片源宽高 + 输出宽高（+ 可选的当前档位），吐倍数和档位；老片源判断是另一个纯函数。**这是这次唯一的真 bug 修复点**：现在的判定从来不看输出，等于假定了一种屏 —— 1080p 源在 1080p 窗口（1.0 倍，放大器纯浪费）和在 4K 屏（2.0 倍，2 倍放大器主场）拿到的是同一条链。

**3.3 档位表取代五个具名组的自动选择。** 表是 Core 里的静态数据加一个纯函数，(倍数档 × 片源类型 × 显卡档) 全枚举钉住。五个具名组**保留为手选覆盖**，选回「自动」就恢复表的结果。显卡档默认低档。

**3.4 色度重建接上每一档。** 全清单里最便宜、也最容易一眼看出来的一项。低档 `CfL_Prediction_Lite`（6.5 KB），中高档 `CfL_Prediction`（13 KB）。**第一批不要同时动 `cscale`**：先保持 `spline36` 不动，等 CfL 确认生效后再单独试 bilinear —— 一批只改一处，不然分不清是谁的功劳。

**3.5 换掉不放大的那一个，删掉转手的那一个。** 大倍数档的 `FSRCNNX_x1` 换成 `ravu-lite-ar-r2`（51 KB，真的 2 倍放大，比现在那 230 KB 还便宜），剩下的倍数继续交给 `scale=ewa_lanczossharp`，不叠第二个神经网络放大器；`Ani4Kv2_ArtCNN_C4F32_i2` 换成上游 `ArtCNN_C4F32.glsl`。

**3.6 `scale` / `cscale` / `dscale` 只由档位链设置。** 画质预设不再碰这三项，设置页不再显示与实际不符的值。缩小档保留 SSimDownscaler 的两条**运行前置条件**（`dscale=mitchell`、`linear-downscaling=no`）—— 那不是附带配置，丢了会比不加更差。

**3.7 每一条链都要能解释。** 开播时往日志写一行，并在高级 / 调试模式下能显示在 OSD 上：倍数、片源类型、显卡档、实际输出尺寸、链上各阶段的文件名。例：`1.33× · 真人 · 低档 · 输出 2560×1440 · ravu-zoom-ar-r2 + CfL_Lite`。没有这一行，用户看片时发现掉帧或画面变了，反馈就没法用 —— 所以它在第一批里，不在最后。**注意第四道闸门**：新加的日志行如果每次不同（尺寸、时间），要么做成确定性的，要么加进 `PROGRESS.md` 那张「允许每次不同」的清单。

**3.8 `NeutralOptions` 一一对应。** 新增的每一个选项都要进那张切组还原表，正反双向那个测试跟着扩。漏一个，关掉那一档之后这个选项会残留一整个文件。

**3.9 新旧并行对照。** 拿新判定跑一遍旧五个组各自的触发条件，把「旧组 vs 新链」的差异**打印**出来（不是断言）。在不能真实播放的前提下，这是唯一能看出行为变化的办法。

### 第一批验收

- 四道闸门全绿：`-c Release`、单节点构建、发布、自检退出码 0 且末行「结果：全部通过」。
- 改了颜色就每个主题一张截图，`daylight` 那张必看。
- 自检报告里 token 出现次数 0。
- 新旧对照的差异清单里，**低档那一列除了「多了色度重建」和「大倍数档换了放大器」之外没有别的变化**。
- 3.0 的结论有单测钉住。

### 第一批做完要用户在屏幕前判的三件

1. **色度重建的效果**：找饱和度高、有大片纯色和硬边的画面（动画字幕、片头 logo、纯色背景上的红字最明显），开关 CfL 前后各截一张。
2. **动画要不要换 ArtCNN**：这是唯一会改画风的改动，所以第一批里**低档默认不换** —— ArtCNN 装箱、进中高档和手选列表，加一个「上一档 / 下一档」的快捷键让用户自己 A/B，他说好再改低档默认。
3. **真人甜点档从 `ravu-zoom` 换成 `ravu-lite`**（同族、上游推荐、整数 2 倍时更锐也更便宜）和**原生档不再过放大器**：这两处不改画风类别，但会变一点。如果用户觉得 1:1 播放变软了，把 `adaptive-sharpen` 加进低档原生格即可。

## 4 档位表：第一批的唯一决策数据

行是倍数档，列是显卡档，默认走**低档**那一列。补足剩余倍数由 `scale=ewa_lanczossharp` 负责。

### 真人

| 倍数档 | 低档（默认） | 中档 | 高档 |
|---|---|---|---|
| 缩小 <1.05 | SSimDownscaler + CfL_Lite | SSimDownscaler + CfL | SSimDownscaler + CfL + adaptive-sharpen |
| 微放大 1.05–<1.45 | ravu-zoom-ar-r2 + CfL_Lite（＝现状 + 色度） | ravu-zoom-ar-r2 + CfL | ravu-zoom-ar-r2 + CfL + SSimSuperRes |
| 甜点 1.45–2.20 | ravu-lite-ar-r2 + CfL_Lite | ravu-lite-ar-r3 + CfL + SSimSuperRes | ravu-lite-ar-r4 + CfL + SSimSuperRes |
| 大倍数 >2.20 | ravu-lite-ar-r2 + CfL_Lite | ravu-lite-ar-r3 + CfL | ravu-lite-ar-r4 + CfL + adaptive-sharpen |

### 动画

| 倍数档 | 低档（默认） | 中档 | 高档 |
|---|---|---|---|
| 缩小 <1.05 | SSimDownscaler + CfL_Lite | SSimDownscaler + CfL | SSimDownscaler + CfL + adaptive-sharpen |
| 微放大 1.05–<1.45 | 现状的亮度部分 + CfL_Lite | ArtCNN_C4F32 + CfL | ArtCNN_C4F32_DS + CfL |
| 甜点 1.45–2.20 | 现状的亮度部分 + CfL_Lite | ArtCNN_C4F32 + CfL | ArtCNN_C4F32_DS + CfL + adaptive-sharpen |
| 大倍数 >2.20 | Anime4K Mode A 六件套 + CfL_Lite | ArtCNN_C4F32_DN + CfL | ArtCNN_C4F32_DN + CfL + adaptive-sharpen |

动画低档那两栏写「现状的亮度部分」是照第 0 节已定的第 4 条：换 ArtCNN 会改画风，得用户 A/B 过才改默认。**读代码确认现在这两种情形实际生效的是哪一组**（大概是 `2K-iGPU-Anime`），把它的亮度部分照搬过来。`ArtCNN_C4F16` 这一轮先不装箱，等低档真要换的时候再取。

**老片源（≤576 线）** 在上表基础上加：低档 `hdeband`；中高档 `hdeband + nlmeans_light`。**开 `hdeband` 必须把 mpv 内置 `deband` 关掉**，两个一起开是互相打架；`hdeband` 排在链的最前面。

**一条链里的硬约束**：顶层亮度放大器最多一个，后置锐化最多一个（`SSimSuperRes` 和 `adaptive-sharpen` 互斥），独立降噪最多一个。着色器包内部自带的多 pass（Anime4K Mode A、ArtCNN）算一个，不算多个。

`CAS` 不在候选里：它的门控是「输出面积和片源面积一模一样才跑」，在这套档位里一格都命中不了（缩小档在缩小，其余三档在放大），和 `FSRCNNX_x1` 是同一类错。要锐化用 `igv/adaptive-sharpen.glsl`，它挂在 `SCALED` 上、没有门控。

八格里实际只有六七条不同的链，缩小档三列之间差别很小，落地时可以合并实现，但表里保持全枚举以便测试。

## 5 第二批：架构（第一批稳定之后再开）

条件：第一批的表不再需要频繁改动、闸门稳定、用户看过画面。这一批**不改变任何一条链**，只让「以后再加着色器」这件事不必重改一整套业务逻辑。

1. **`ShaderDescriptor`**，最小字段集：名字、文件名、职责（前处理 / 去带 / 降噪 / 亮度放大 / 色度重建 / 后置锐化 / 缩小）、`ChangesResolution`、hook 阶段、读写 luma / chroma 四个布尔、相对成本、是否内部多 pass。**`ChangesResolution` 是这批里最有价值的一个字段** —— 它是能机械地拦住 `FSRCNNX_x1` 那类错误的唯一手段，别靠文件名猜「它是不是放大器」。相对成本别自己编等级：用 mpv 统计页里每个 pass 的**实测耗时**定，在这台机器上量一次就有真数。
2. **`ShaderChainPlan`** 作为判定的正式输出，渲染层只执行、不决策。
3. **一个入口**：`Plan Resolve(Context)`。UI、视图模型、Service 都不再各自拼链、各自判断「这个显卡能不能跑这个着色器」。
4. **三条互斥做成校验 + 单测**（放大器 / 后置锐化 / 降噪各 ≤ 1，`hdeband` 与内置 `deband` 互斥）。判断针对 Descriptor 的逻辑职责，不是数底层 pass 文件个数。
5. **渲染层契约测试。** Plan 里没有的着色器和选项，渲染层不许偷偷加；Plan 说 `deband=off`，渲染层必须真写成 off 或等价中性值；Plan 指定的 SSimDownscaler 前置条件必须完整落地。这个项目已经栽过一次同类问题（画质预设写 bilinear、组随后写 spline36、组总赢、界面在骗人），这条测试就是防它复发的那一层 —— 「Core 决策正确但渲染层跑成另一条链」只有这一层能抓住。
6. **`vo` / `gpu-api` / `hwdec` 纳入同一套档位。** ArtCNN 和 CfL 的官方说明都写 `vo=gpu-next`；mpv-prescalers 的 README 记了一条具体故障：`vo=gpu` 配 `gpu-api=d3d11` 会报 `rgba16f` 格式不可用。所以 **`gpu-next` 是默认，`gpu` 只当回退**；AMD 上 `gpu-api` 优先 vulkan，但不许硬编码成「所有机器必须 vulkan」；`hwdec` 保持 `auto-safe`，但要确认硬解出来的帧能正常进链。
7. 迁移方式：**新模型先行**，旧五个组在这批之后只当手选覆盖，不再承担自动判定；确认稳定后再删旧实现。不要先删旧组再慢慢补新的。

## 6 明确不做的，以及将来什么条件下再做

上一版里下面这些是「强制」，这一版明确不做 —— 不是忘了，是现在做了没有用处。写在这里免得下个窗口又把它们加回来。

- **`QualityMode`（Performance / Balanced / Quality）。** 规定它不能突破显卡档预算，而低档每格只有一条固定链、没有候选集合，所以在这台机器上三种模式选出来的东西完全一样：一个永远不生效的设置项，加一批永远为真的测试。**再做的条件**：某一格真的有两条以上都在预算内的候选链。
- **`ColorSpaceHint` / `ColorContext` / `DynamicRange` / `ProcessingDomain` 四个枚举。** 这批着色器要么是 Passthrough、要么资料不足，而这两者行为一致；`ColorTransform` 又明确说本轮不实现，于是 Plan 里会留一个渲染层无法执行的字段，再要求契约测试保证它永远是空的。这份清单里唯一真和 transfer domain 有关的要求（SSimDownscaler 那两条）已经用「运行前置条件」表达掉了 —— 说明正确的抽象是「着色器的前置 mpv 选项」，不是一套自建的颜色管线。**再做的条件**：出现一个明确要求线性光域的着色器，或者播放器真有了显式的色彩转换能力。**但「不许靠文件名猜色彩空间」这条规则留着**：放大器只做空间重建，不要因为选了 ArtCNN / ravu / Anime4K 就自动加伽马↔线性转换。
- **显卡自动检测（Auto）。** 上一版一边禁止型号字符串进 Core，一边说型号映射「只能放在能力检测层」，等于把同一张表搬了个家；而在唯一能验证的这台机器上，检测结果就是低档。**再做的条件**：换了机器，或者播放器能拿到丢帧统计 —— 那时候做「持续掉帧就降一档」，它不需要型号表，而且能在本机验证。
- **`KrigBilateral`**：上游没有许可声明，不装箱。
- **约束求解器 / 候选集合筛选**：十格里实际只有八九条链，用不着求解器去选一条。
- **`FSR`、`SGSR`、`NVScaler`、`nnedi3`、当前表格没用到的 ArtCNN 变体**：不装箱。要用再取，上游一直在那儿。

## 7 测试清单

- **档位边界**：1.00 / 1.049999 / 1.05 / 1.449999 / 1.45 / 2.20 / 2.200001，以及浮点误差和整数分辨率带来的典型倍率。1.00 要落缩小档但 OSD 标签是「原生」。
- **片源 → 输出组合**：1080p→{1080p, 1440p, 4K}、720p→{1080p, 1440p, 4K}、480p→{1080p, 1440p, 4K}、576p→{1080p, 1440p}、4K→1440p。**576p→1080p 必须落甜点档、且仍然带去带** —— 这是 2.2 那条独立轴的回归测试。
- **回差**：1.44→1.46 不换档；1.44→1.52 换档；在 1.44 和 1.46 之间来回时结果稳定。
- **窗口**：连续 resize 期间一次都不重新生成链；稳定后不跨档只更新输出尺寸；跨档才重新生成；进 / 退全屏用预案且不等防抖；换显示器按离散事件重算；小窗↔大窗两个方向都测。
- **表的完整性**：每一格 (倍数档 × 片源类型 × 显卡档) 都能解出一条链，且链里每个文件都在 `assets/shaders/` 里 —— 扩展现有那个「两处不许漂移」的测试。
- **`NeutralOptions` 正反双向还原**：`A→B→A` 和 `B→A→B`，覆盖所有档位用到的选项，确认着色器文件、mpv 选项、链专属选项都不残留。
- **三条互斥恒成立**；`hdeband` 在链里时内置 `deband` 必须是 off。
- **3.0 那个 CfL 顺序的结论。**

## 8 上一版里被改动的地方

给下个窗口对照用 —— 照这份做，不要照那份。

- **上一版第六节「三件已定」里第二、三条被改成了与用户 2026-09-03 决定相反的内容**（动态重算、默认 Auto），而标题还挂着「用户授权我定，第三件是他指定的」。已按用户 2026-09-03 二次确认的版本恢复：实际输出尺寸 + 全屏双预案；显卡档默认低档、本轮不做 Auto。
- 倍数档从四档改五档（加 `Native`），并加 0.05 回差。
- 去带 / 降噪从大倍数档里拿出来，改成「片源高度 ≤576」的独立轴（PAL / NTSC 同一张 DVD 处理不同的那个 bug）。
- 动画低档保持现状，ArtCNN 先只进中高档和手选列表，等用户 A/B 过再改默认。
- 上一版第八节那 1400 多行按第 5、6 节收拢：留下有牙的部分（`ChangesResolution`、三条互斥、渲染层契约测试、单一文件来源、上游扩展名、`NeutralOptions` 双向、新旧对照），去掉现在不干活的部分（`QualityMode`、四个颜色枚举、GPU Auto、约束求解器）。
- 上一版第四节点名的 `Restore_CNN_M` 不在「Anime4K Mode A 六个文件」里（Mode A 用的是 VL 那版），清单里也没有它。本版缩小档和原生档不加线条修复，这个漂移随之消失。
- 上一版把同一套规则写了四遍（正文、十条原则、八条契约、三十个勾选框），已经开始互相矛盾。本版每条只写一遍。

**2026-09-03 晚，读了工作树之后又改的三处**（前一稿是我在没读工作树的情况下写的）：

- **第五档 `Native` 撤掉**，改成缩小档里的一个 OSD 标签。理由是 `SSimDownscaler` 自带门控，1:1 时本来就不出手，为一个标签单开一档不值。
- **`CAS` 从候选里去掉**：门控是「输出面积等于片源面积才跑」，这套档位一格都命中不了。
- **微放大档标注了 `ArtCNN` 的 1.3 倍门控**：那一档 1.05–1.30 段 ArtCNN 不出手。动画低档那两格照旧「保持现状」，因为他最常见的 1080p→1440p 是 1.33 倍，现在那个放大器在这个点上是会出手的 —— 别为了避开死区把这一格改成 ravu-zoom，那会把他每天看的动画降级。





