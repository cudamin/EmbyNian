# 交接：集成播放器连续缩放与起播自动全屏

保存日期：2026-09-22。原会话：`sess_8f90f701-b786-47c9-ba75-bcb628642fc2`。

## 先看结论

**任务尚未完成，不能把当前工作树当作已修好的版本交付。** 代码能构建，单测通过，但加强后的本地播放探针仍在连续缩小阶段失败；新增的自动全屏实播阶段因前面的失败尚未执行到。本轮没有发布、没有提交、没有推送。桌面快捷方式仍运行本轮修改前的发布件。

用户最新要求是「保存当前任务进度，方便其他 agent 衔接」，所以这一轮只保存交接，不继续试验或发布。

最重要的接手线索：**`CompositionSurfaceBrush.Stretch=Fill` 加 `SpriteVisual.Size=目标尺寸` 并不保证交换链像素被缩放。** 临近交接才重新找到 `work/全屏卡一下-诊断与修法.md`：2026-09-17 的半尺寸实验明确测出这条管线忽略这两项的缩放效果。当前生产 `CompositionVideoTarget.ApplyFrame` 恰好只写目标 Size、Offset，Scale 恒为 One。离散全屏由原有 `VideoFrameOverlay` 快照遮住了，连续拖动没有这个遮挡。**应优先复核这个真实像素问题，不要继续只围着 XAML 尺寸读数打转。** 历史实验不是本轮复测结果，需要在当前 SDK/libmpv 上重新验证。

## 2026-09-22 深夜续（本文件之后有人接了一手）：判据换成屏幕像素、根因量清，**放大方向仍未修好**

完整记录见 `PROGRESS.md` 顶部「集成播放器缩放：判据换成屏幕像素；根因量清（三尺寸分家）」条目。要点：

- **旧判据里「布局滞后」那半条是框架的账、永远为真**（实测 24 拍全是 `SetWindowPos` 返回时的立即采样，Delay(16) 的下一拍就追上），已换成**屏幕像素**判据（拖动四段结束时读客户区横线上四点，素材六条等宽彩条，右端那点直接回答「铺满没有」）；四段连续缩放现在全绿，`PlayerPage.ClientRect.cs` 里那段手动排树试验已删。
- **根因：交换链描述符 / `d3d11-composition-size` / 缓冲里像素的真实尺寸是三个会分家的数。** 窗口不动、只改 composition 尺寸的对照里，描述符一动不动而画面真的缩了一半；`SpriteVisual.Size` 与画笔 `Stretch=Fill` 都不缩放像素，`visual.Scale` 生效。真实播放页上放大一记 1.5 倍之后画面仍是「旧像素一比一贴在左上角」（录像 `work/resize-startup-capture-211213-screen2/frame-0230.png`），探针的老化曲线把每一拍都记下来了。**新加的「大幅快速缩放」判据当前红，已降级为诊断**（不阻断其它检查）。
- **顺手修好一条真实缺陷（已像素验证）**：保留最后一帧的摆放（`RefreshRetained`）把 `_retainedShape` 那个 1.777×1 的**比例**当裁剪框用。红/绿对照留在探针里 —— 旧算法读数 `KKKK`（一片糊），新算法 `RGMC`（真实画面）。
- **接手第一步建议**：在真实播放页上做一次**受控的** `PinProbePlacement` 像素对照（把摆放钉死成已知值再读屏幕），先把「Scale 在追赶期到底生不生效」钉死 —— 那是这套修法的前提。两条候选修法（画笔 `Stretch=None`＋`TransformMatrix`；或「固定输出尺寸」让 mpv 一直按显示器尺寸出帧）都写在 PROGRESS.md。

## 用户原始要求与范围

1. 集成模式放大窗口时，画面短时不能铺满；缩小时，画面短时被裁切。让调整窗口更顺滑。
2. 集成模式开始播放后自动全屏不够顺滑，重新设计过渡动画。

实现方向由本会话自行选择，用户没有单独确认技术方案：保留手动全屏的直接切换，不添加手动全屏缩放动画；起播全屏改成加载层完整交接后淡入首帧，避免整页缩放、窗口切换和揭幕重叠。若接手发现方案不能解决像素问题，可以调整实现，但别误称用户已批准某种架构。

只改集成管线，不扩大到独占/uosc。真实 Emby 播放未获授权；所有本轮播放验证均为隔离本地探针，不登录、不上报观看历史、不改用户设置。

## 工作树保护

- 工作目录：`C:\Users\89400\EmbyNian`，分支 `master`，HEAD `9b9b6f0`。
- 进入本会话时已有大量其他任务未提交改动：字幕字体/筛选/音量/统计/光标自检等。**不能用整树 reset/restore 回滚。** 尤其 `PlayerPage.xaml.cs` 和 `LibMpvBackend.cs` 原本就有别人改动。
- 本轮开始时保存了相关文件副本：`work/resize-startup-before/`，共 13 个文件，按源码相对路径放置。
- 本次交接另外保存：`work/resize-startup-handoff-20260922/`，包含本轮范围的当前源码快照、与开始副本相比的 `session.patch`、验证日志/报告、`state.json` 和当前 git 状态。
- `session.patch` 是当前文件减会话开始副本，不是减 HEAD；用于审查或定点撤销本轮试验，**不要在当前树再 apply 它**。
- `PlayerPage.Input.cs` 曾备份，但本轮没有改它，当前与开始副本相同。Git 中它的差异属于其他任务。
- 交接时没有 EmbyNian 进程运行，没有未完成的后台构建/捕获任务。三个只读代理都被 provider rate limit 阻断，**没有成功的独立代码审查结论**。

## 已经写进工作树的修改

以下是草稿实现，不等于视觉验收通过。行号以交接时源码为准。

### 1. 原生客户区直接通知视频层

- `src/EmbyNian.Shell/Windowing/HostWindow.cs:727`：新增 `ClientSizeChanged`。
- 同文件约 `2890`：非最小化、非 `ChangeClientRect` 内部的 `WM_SIZE`，若不是最大化态改变，就发布真实客户区；离散最大化仍走原来的 `ClientRectTransition`。
- `Views/PlayerPage.xaml.cs`：Attach 订阅，Shutdown/Detach 两处退订。
- `Views/PlayerPage.ClientRect.cs:182`：`OnClientSizeChanged` 先调用 `_videoTarget.ResizePresentation(client)`，再对本页、Root、VideoHost 分别 Measure/Arrange，最后刷新呈现。
- **最后这一组手动布局是尚未奏效的试验，当前仍留在树中。** 它没有解决立即采样时的布局滞后，应重新评估，不要把它视作已验证方案。

### 2. 缩放时不再反复推迟 100ms

- `Windowing/CompositionVideoTarget.cs`：`_debounce` 改名 `_geometryTimer`，周期从 100ms 改为 16ms、单次。
- `QueueGeometry` 只在计时器没有运行时 Start，后来的尺寸不 Stop/Restart，所以持续拖动也持续派发。
- 新增 `ResizePresentation`（约 514）：记录来自 HWND 的目标客户区和时间，调用 `SetSize`/`Schedule`。
- `CurrentFrame` 的目标客户区分支也传 `_resizePending`；原先这里硬传 false，会在连续调整时反而走 contain。
- 原有离散切换 `SnapPresentation` 仍清 `_resizePending`，保留快照/缓冲/布局交接机制。
- `Core/Playback/IVideoSurface.cs` 文档改成逐拍合并，不再称 resize 防抖。
- **实际像素缩放尚未改**：`ApplyFrame` 仍是 `visual.Size = target/raster`、`Offset = target/raster`、`Scale = One`；画笔仍是 `CompositionStretch.Fill`。这是最值得接手先验证的缺口。
- 文件里部分旧注释还在描述「拖动仍是 100ms 防抖」「信窗口期间不看 resizePending」，已经和草稿不一致，正式定案时清理。

### 3. 后端合并尺寸更新任务

- `Core/Playback/LibMpvBackend.cs:1019` 起：`OnGeometryChanged` 从每次 `Task.Run(RefreshComposition)` 改为 `_compositionRefreshPending` / `_compositionRefreshRunning` 两个 Interlocked 标记，只保留一个后台工人，循环读最新 Size。
- finally 放掉 running 后重查 pending，防止丢掉最后一个尺寸。
- `RefreshComposition` 仍在 `_apiGate.Run(..., composition: true)` 内，销毁/StopComposition 后返回空，不触碰已释放 mpv。
- 生命周期门类型不是独立文件，位于同一文件末尾 `LibMpvLifetimeGate`（约 1350）。
- 这部分目前只有整体构建和原有生命周期测试，没有新增专门针对这两个 pending/running 标记的并发测试。

### 4. 起播全屏改为串行交接

- `Core/Playback/PlayerMotion.cs`：新增 `FullscreenEnterMilliseconds=160` 和 `FullscreenEnter(Pose,long)`，只动画 Opacity，Scale=1、OffsetY=0；`CoverMilliseconds` 180→240（当前是共享常量，所以换集揭幕也一起变长了，接手应确认这是否合适）。
- `Views/PlayerPage.Transition.cs`：移除第二颗 fade-handover 计时器、50ms/20 次重排等逻辑；`TransitionPage` 只留一颗姿势时钟，完成时先归一，再调用 landed。
- 新增 `_startupHandoverPending` 和 `CompletePlayerEntranceAsync`（约 74）：
  1. 提交已不透明的页面，`RequestCommitAsync`（350ms 上限）+ `DwmFlush`；
  2. `_shell.ShowPlayer(true)`、`VideoVisible=true`；
  3. `EnterAutoFullscreen()`，await `_windowChange`；
  4. 再确认提交；finally 清 pending，然后让遮罩检查首帧。
- `Views/PlayerPage.xaml.cs:703` 附近：EnterPlayer 开 pending，按真实播放生命周期与自动全屏设置选择 FullscreenEnter，landed 调上述交接。
- `OnPlaybackStarted` 在 pending/enterAnimating 时不抢跳全屏；`OnPictureAspectChanged` 在 pending、待全屏或 CoverUp 时不先把窗口 FitToPicture 成中间尺寸。
- `Views/PlayerPage.ClientRect.cs:66`：Cover 可见时不抓视频快照，防止 `VideoFrameOverlay` 突然把首帧亮到加载层上方。仍等待最终布局；已有链时再等实际缓冲，没有链时只等布局。
- `CancelWindowChange` 同时清 startup pending、递增 generation，旧异步回调通过 Current 守卫失效。
- **待复核**：用户在 pending 期间按 Esc/手动窗口化，后来的 EnterAutoFullscreen 是否又覆盖用户最后选择；catch 回退重试自动全屏的语义；共享状态与快速重入的竞态；无动效时的流程。本轮尚未成功跑到自动全屏实播阶段，不能声称已验证。

### 5. 遮罩就绪判据

- `Views/PlayerPage.Cover.cs:74`：PictureReady 增加无 pending、窗口任务完成、入场动画完成，之后才查真首帧和缓冲尺寸。
- `HideCoverPlate` 不再因为系统禁用动效就提前去掉遮罩；禁用动画改为就绪后直接收起。
- 重复首帧状态通知不重置 `_revealStarted`，也不重新启动已经在淡出的遮罩。
- 6000ms 保险丝仍存在。当前 `PictureReady` 仍使用 HasPicture + IsContentReady，布局稳定由入场交接负责。

## 测试和探针变更

### 单测

`tests/EmbyNian.Tests/PlayerMotionTests.cs` 新增两条：自动全屏进场只溶解、不缩放/位移；中途重入保留透明度、不继承退场缩放。

### 本地真实播放探针

`src/EmbyNian.Shell/PlayerMotionProbe.cs` 和 `Views/PlayerPage.MotionProbe.cs` 已增强：

- 缩放前 `ProbePictureAspect(16/9)` 真正锁成视频形状；旧探针居然在 1064×792 的宿主上缩放 16:9 素材。
- 四段各 24 次：缩小、放大、固定右下角拖左上缩小、反向放大。
- 每次 SetWindowPos 返回立即采样，再 Delay(16) 后采样，共每段 48 拍。
- PlacedRect 和 HostSize 都对照 `window.ClientSize`，不是两份同样晚到的 XAML 尺寸互证。记录 GeometryChanged 派发次数，要求拖动中持续更新。
- 增加即时诊断：LayoutSlot、ActualSize、父 Visual.Size、标题按钮右边、XamlRoot.Size。
- 后面新增「自动全屏起播，首帧与窗口交接同时到达」：真本地后端、真实 PictureStarted，统计提前揭幕、整页变换、快照穿透加载层，结束时走 ReturnToBrowseBeforeStopAsync。
- 探针总保险丝 55→75 秒，未新增 CLI 开关。
- **当前会在第一段连续缩小就抛异常，后续放大/快速全屏/新起播阶段没有执行到。** 不要把“阶段写了”说成“阶段通过了”。

## 最新验证结果与证据

### 当前代码构建与单测

- `work/resize-startup-build-5.log`：完整 Release 方案构建 **0 警告、0 错误**，46.89 秒。
- `work/resize-startup-tests-5.log`：**998 项通过，失败 0，跳过 0**，10880ms。
- `work/resize-startup-whitespace.log`：较早（build-2 之后）跑过空白检查 exit 0；之后继续改了生产代码和探针，**最终版本还需再跑**。
- 未执行本轮闸门 3 publish，未执行本轮闸门 4 selfcheck-diff。原 PROGRESS 中 `播放回来那一页` / `跨季相邻单集` 是其他既有红，本轮没有验证其变化。

### 副屏本地探针仍失败

最新：`work/resize-startup-probe-screen2-run4.txt`；录像/帧：`work/resize-startup-capture-202827-screen2/`；捕获入口日志：`work/resize-startup-screen2-run4.log`。

关键实测：

```text
sample=1 client=(1048,589) host=(1064,598) raster=1
root=1064x598 island=1048,589
slot=0,0,1064,598 actualSize=<1064,598> visual=<1064,598>
缩小取样：48 拍，画面滞后 0 拍，布局滞后 24 拍，尺寸派发 21 次，耗时 742ms
失败：缩小每拍画面和布局都贴住真实客户区，不留空或裁切
```

“画面滞后 0”只表示 **SpriteVisual 属性反算的目标框** 正确，不代表交换链像素真的缩放。布局旧值都发生在 SetWindowPos 后的立即采样，下一拍通常追上。LayoutSlot 与父 Visual.Size 也旧，不能简单把失败豁免为 ActualWidth 异步缓存；但画面究竟因此如何受裁剪，仍须像素实测判定。

已尝试且没有解决的方案：

1. WM_SIZE 回调 `_window.SynchronizeContentLayout()`，再刷新目标 Visual。
2. 把 HostWindow.SynchronizeContentLayout 的 UpdateLayout 挪到 Measure/Arrange 前。**无效且已按 diff 定点撤销，HostWindow 该方法目前恢复原顺序。**
3. 不排全树，直接本页、Root、VideoHost 分别 Measure/Arrange。**无效，目前留在 ClientRect.cs 作为最后试验**。

前几次证据：

- `work/resize-startup-probe-screen2-run1.txt`，捕获 `resize-startup-capture-201722-screen2/`。
- `work/resize-startup-probe-screen2-run2.txt`，捕获 `resize-startup-capture-202213-screen2/`。
- `work/resize-startup-probe-screen2-run3.txt`，捕获 `resize-startup-capture-202614-screen2/`。
- 看过 run1 的 `resize-sheet.jpg`，缩小时测试图在移动；仅这张拼图不足以证明无裁切。其余无需重新遍历几百张，先用确定的颜色边界/固定缓冲实验定位。

### 修改前参照

- `work/player-motion-capture-192413-screen2/`：启动原 artifacts 发布件，旧版探针报告“全部通过”，说明旧探针有覆盖缺口。
- `work/resize-startup-baseline-classification.txt`：旧 `check-transitions.py` 的分类，不能作最终验收；它只比较彩色 bbox、容差大，对 crop 不敏感。
- `work/resize-startup-baseline-screen2.log` 显示进程 exit=1，但旧探针报告全部通过。原捕获脚本在 HWND 消失而进程仍清理时会 terminate 自己启动的进程，可能造成这个退出码，不能据此认定产品崩溃。
- **新版尚未在主屏执行任何本轮捕获**。

## 接手优先路线

1. **先复测交换链像素缩放，而非继续调整多层 Measure/Arrange。** 读 `work/全屏卡一下-诊断与修法.md`（历史证据，第 4 条最重要），以及 `src/EmbyNian.Shell/CompositionPlaybackProbe.cs:146` 起的半尺寸实验。它会直接设置 `d3d11-composition-size=1280x720` 并逐点取屏幕颜色，操作前通读，确认不会被新 geometry worker 立刻覆盖实验尺寸。
2. 对于老缓冲如何填入新客户区，比较可工作的实际呈现方式：显式 Visual/父级缩放或原生 SwapChain 矩阵，以及 SwapChainPanel。**本轮还没实现任一种，不要假定 SetMatrixTransform 必定解决**：微软文档说明它在下一次 Present 应用，且需要与 mpv ResizeBuffers/尺寸变化同步，暂停状态也必须覆盖。
3. 本地参考源码已存在：`work/ref-mpvw/mpv-winui/mpv-winrt/MpvPlayer.cpp:1279–1318`，使用 `SwapChainPanel` + `ISwapChainPanelNative.SetSwapChain`，按 CompositionScale 设置 `IDXGISwapChain2.SetMatrixTransform`。它是 DPI 补偿示例，不是本题现成答案。
4. 先用固定尺寸缓冲 + 变化客户区 + 显眼边框/测试图证明没有 crop/空白，再回到实际动态缓冲。不要依赖 `PlacedRect` 为绿；不要靠快照冻结整段拖拽冒充视频持续顺滑。
5. 用增强探针跑完四向缩放、起播首帧/全屏竞态、暂停缩放、快速反向与退出清理。必要时把新起播阶段单独安排在会失败的缩放前，以便独立验证，但不能删除或放宽失败来制造通过。
6. 最后清理无效试验和过期注释，做缺陷审查、重新跑构建/测试/空白，再串行更新实际 publish。用户没有授权 git commit/push。

## 命令、素材与捕获工具

必须先读根 `CLAUDE.md` 和 `.claude/skills/embynian-playback/SKILL.md`、`embynian-verification/SKILL.md`。所有命令在当前工作树根运行；SDK 不用 PATH 上旧版。

```bash
"$HOME/.dotnet/dotnet.exe" build ./EmbyNian.sln -c Release --no-restore -m:1 -p:BuildInParallel=false -p:UseSharedCompilation=false -p:MSBuildNodeReuse=false
"$HOME/.dotnet/dotnet.exe" run --project ./tests/EmbyNian.Tests/EmbyNian.Tests.csproj -c Release --no-build
"$HOME/.dotnet/dotnet.exe" format whitespace ./EmbyNian.sln --verify-no-changes --no-restore
py -u work/capture-resize-startup.py 2
py -u work/capture-resize-startup.py 1
```

- 素材：`work/player-motion-fixture.mp4` 和同名 `.png`，本地 lavfi 测试源，16:9。均存在。
- `work/capture-resize-startup.py`：本轮从旧 fast 脚本派生，启动 **Shell/bin/x64/Release/net10.0-windows10.0.19041.0/win-x64/EmbyNian.exe**，运行 `--probe-player-motion`；不会运行 artifacts 的旧版。捕获最长 90 秒，等待进程退出 15 秒；finally 只清自己启动的进程。不要并行跑两屏，否则激活/全屏会干扰。
- 抓帧目标频率写 60，但本机 GDI/磁盘实际常约 25–35fps。MP4 是按 60fps 编码，**时间判断请用 capture.txt 实际时间戳，不要把 MP4 时长当真实耗时，也不能声称实测 60fps**。
- 副屏 1080×1920，factor=0.375；主屏 2560×1440，factor=0.5（运行时仍核实显示器）。脚本会遮掉客户区以外的屏幕区域，所以无法据此检查目标区域超出当前 HWND 的所有像素。
- 原始日志默认在 `%LOCALAPPDATA%\EmbyNian\logs\player-motion-probe-<stamp>-<pid>\logs\player-motion-probe.txt`，本轮已将各失败报告复制到 work。
- `work/update-resize-startup-probe.py` 是一次性变更脚本，**已经执行过，别重跑**，它会因旧字符串不再匹配而失败；接手直接改源码。
- 旧 `work/capture-player-motion-fast.py` 启动 artifacts，不要误用来验证当前代码。
- `work/check-transitions.py` 可作粗筛，不能代替逐帧验证，也不能把其 `other` 一律当正常。

验收后才执行（交接时未执行）：

```bash
powershell -NoProfile -ExecutionPolicy Bypass -File tools/publish.ps1 -NoArchive
powershell -NoProfile -ExecutionPolicy Bypass -File tools/selfcheck-diff.ps1
```

publish 前确认程序未占用目标目录，不擅自杀用户进程。普通 selfcheck 可能读取真实服务器，不能称离线；本题无需真实播放。不要为交接文档或未完成草稿覆盖发布件。

## 发布件与当前构建的区别

交接时 SHA256（进一步改动后自然会失效，只供识别这一版）：

- 当前开发构建 `EmbyNian.dll`：`f297ff1e064a00afdcde2813a4d480f6c6cedcc63d952641fa9f2d1d66b1d906`。
- artifacts 发布件 `EmbyNian.dll`：`dd30dcc04ef0cad08a6a59694caa82198a8e9f38fb40f6de7e1a311eabd7c1bd`。

微软参考：

- [CompositionSurfaceBrush](https://learn.microsoft.com/windows/windows-app-sdk/api/winrt/microsoft.ui.composition.compositionsurfacebrush)
- [IDXGISwapChain2.SetMatrixTransform](https://learn.microsoft.com/windows/win32/api/dxgi1_3/nf-dxgi1_3-idxgiswapchain2-setmatrixtransform)
- [WinUI ActualWidth 的异步布局限制](https://learn.microsoft.com/windows/windows-app-sdk/api/winrt/microsoft.ui.xaml.frameworkelement.actualwidth)

**收尾状态：已保存、可编译的未完成草稿；真实缩放问题没有完成验收，新自动全屏实播未跑到；不提交、不发布。**
