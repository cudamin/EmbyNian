# 开发进度

最后更新：2026-08-31

> **做完的活按月归档。** 这一轮的细节、上一轮的细节，全部在 [`docs/progress-2026-08.md`](docs/progress-2026-08.md) 里，原文一字未改。留在这个文件里的只有活着的东西：手上的活、还没解决的、验证入口，加一份「最近做完了什么」的索引。原先它长到 387 KB，而每个新窗口都要把它整本读一遍。
>
> 下文凡是「见下面第一件活」「见下面那件活」这类指路，除了本文件里真有的那一节，指的都是归档里最上面那几件（新的在前）。

## 在途工作（中断后从这里接）

这一节写给「上一个窗口的上下文丢了」的读者，包括未来的我。**它是唯一的交接文档**：只有写进这个文件的东西能跨窗口活下来，会话记录、后台 agent、工作流的 run id 都不能。所以这一节要在每个工作单元落地时更新，而不是等一个阶段做完。

**在途：这一轮是「检查当前项目给出优化建议」，先后顺序用户交给我定（「你来决定」）。** 复核出七条，**七条现在全部做完了** —— 第五条当时议定押后，后来用户又把它挑回来，也在这一轮里做完了（见下面「第五条」那一节）。逐条：

- **一、整次 v3 重写还没进版本库（做完了）** —— 分支 `winui3-rewrite`、`bc8edbd`、260 个文件。`master` 还停在 v2 那一版，一个字没动。截图（`shots/`、`docs/shots/`、`shell.png`）按 `.gitignore` 故意不入库：五十多兆进了历史就再也出不来，留在磁盘上照旧能看。要把 `master` 挪上来只是一句 `git merge --ff-only winui3-rewrite`，**用户后来点头了，已经挪过去**（见下面「接下来动哪些」那一段）。
- **二、三处用户看得见的毛病（做完了，`2366f95`）** —— 封面有时永远空着、详情页开得慢、滚动卡顿。为什么这么修写在那条提交说明里，不在这儿重复；新增 14 项测试钉住「放弃的一方等到的必须是取消，不是空答案」和淘汰顺序。
- **三、发布件能瘦 40 MB（做完了，`32b76cf`）** —— 细节在归档里「发布件瘦掉 52 MB：拆开 Windows App SDK 那个总包」那一节。实际瘦了 52 MB：拆掉 Windows App SDK 那个总包，`onnxruntime.dll` 加 `DirectML.dll` 那 39 MB 的推理运行时不再跟着走。`libmpv-2.dll` 实际 117 MB（注释里那个 36 MB 的老数字一并改对了）；**换一个小些的 libmpv 构建要用户点头**，着色器和解码路径都吊在它身上 —— 用户此前明说过「libmpv-2.dll不用换」。
- **四、开发回路可以快一半（做完了，`272d00b`）** —— `tools\publish.ps1 -NoArchive`。同一次发布从一两分钟降到 22 秒。
- **五、结构失衡（做完了，`8a83b3b`＋`de3608a`＋`d896c3e`）** —— 当时是三处：`ShellSelfCheck.cs` 3602 行、播放页自检另有 1783 行；Shell 那 35735 行一项单元测试都没有（测试项目只引 Core）；头图罩子的渐变档位在 XAML 和 Core 里各写了一份。押后是当时议定的，后来用户又把它挑回来，三处都做完了 —— 见下面「第五条」那一节。
- **六、仓库和文档卫生（做完了）** —— 见 [`docs/progress-2026-08.md`](docs/progress-2026-08.md) 里「仓库瘦身、进度记录搬家、日志改成常开句柄」那一节。
- **七、两件已知没修的（做完了）** —— 细节在归档里「补上验证工具那两个窟窿」那一节。`--scroll-half` 从前在带子还没装好时就量、偏移被夹回 0，现在先等页面长够了再滚，落点读回来写进日志（「目标 230，可滚 1687，落在 230」）；命令行上新增 `--theme <id>`，六套配色都能一条命令拍一张，而且只压这一次运行、不写设置文件。这个开关立刻抓到一处浅色主题下才看得见的毛病 —— 设置窗口标题栏右端那三颗系统按键底下是一块黑的 —— 已修。**顺手更正一处旧错：浅色主题只有「晴昼」一套，此前这里写的「三套」是错的**（六套里五套深色）。

**七条走完之后问了一次「接下来动哪些」，用户挑了三件**（原文照抄，逗号也是它给的）：`修窄窗口下的下拉被切,第五条：结构失衡,把 master 挪到新版本上`。三件的下场：

- **把 `master` 挪到新版本上（做完了）** —— `master` 从 `9e4377b` 快进到 `4fad063`，此后每次提交完照原样再快进一次，**所以它和 `winui3-rewrite` 总是同一笔**（别在这儿抄 sha，抄了就得跟着改）。**没有 checkout、没有 reset**：先证明 `master` 是 `winui3-rewrite` 的祖先、反向也没有落下的提交，然后一句 `git push . winui3-rewrite:master` 直接改引用，HEAD 一直待在 `winui3-rewrite`、工作树全程干净。整次迁移的未提交状态不许动，这是唯一安全的挪法；以后每次提交完照这句再来一遍。
- **修窄窗口下的下拉被切（做完了，`734f752`）** —— 细节搬到归档里「窄窗口下那三个下拉不再被切 —— 排不下就换行」那一节了。
- **第五条：结构失衡（做完了，见下面「第五条」那一节）** —— 三件事：头图罩子的渐变档位在 XAML 和 Core 里各写了一份，现在只留 Core 那一份、XAML 从它生成（`8a83b3b`）；Shell 那 35735 行一项单元测试都没有，四段纯算术搬进 Core，测试从 489 加到 511（`de3608a`）；`ShellSelfCheck.cs` 3602 行加播放页自检 1783 行拆成 partial，一共十六个文件（`d896c3e`）。**注意它和前六条不一样：用户看不见任何变化，所以判它成不成只能靠闸门 —— 四道全绿加自检报告逐行和改之前对得上，才算没把东西改坏。**

**这一轮到此为止：手上没有在途的活。** 用户挑的三件全部落地，第五条自己那三处也全部落地（`8a83b3b`、`de3608a`、`d896c3e`），每一处都是四道闸门加自检报告逐行比过的。下一件活等用户开口。

下面这一段是上一轮（重绘界面）收尾时的状态。留着是因为里面的约定和那两处偶发还管用：

**在途：重绘前端界面。** 用户这次的话原文照抄：「开始任务之前备份当前版本，方便后续恢复。前端界面太丑了，重绘一个，然后生成几套主题。」拆成三件：

- **备份**（**做完了**）：`C:\Users\89400\EmbyNian-backups\EmbyNian-20260830-0434-重绘界面前.zip`，266 个文件 + 1 个说明，75.9 MB。要回退整次重绘就解它。
- **几套主题**（**做完了**，见下面第一件活）：六套，设置页选中即生效。
- **重绘**（**做完了**）：方向和单元划分写在下面「重绘界面的方向和单元划分」那一节。**单元 2（词表）、单元 3（外壳）、单元 4（卡片和卡片带）、单元 5（详情页头图 + 主页轮播的版式）、单元 6（设置页卡片 + 主题色板）六个单元全部落地，各自过了四道闸门**，见下面第一件活起的五件。整次重绘到此为止 —— 再动界面就是新的活，不是这一条的余下部分。
- **插进来的一件（**做完了**）**：「红框框出来的地方全填充上海报，海报要用最近添加」—— 主页那条大图铺满窗口顶上那一整块，标题栏和页眉浮在图上，图源换成最近添加，见下面第一件活。
- **重绘之后又插进来的两件（**都做完了**，见下面第二件活）**：「点击主页继续观看、媒体库、最近添加的封面之后会先跳转到页面下方，然后才会进入页面」（横带自己露出卡片，不再往上冒 `BringIntoView`）和「往下拉之后标题颜色要渐变，变的和下方背景一样」（标题栏那一条按罩子自己的曲线洗成正文的纸色，外壳那一层底色一并撤掉）。
- **再插进来的一件（**做完了**，见下面第一件活）**：「锁定主页的窗口长宽避免确保调整窗口大小时候，轮播画面不会被裁切，在设置中新增锁定窗口哔哩大小和默认收起侧边栏的开关选项」—— 带高的上限从写死的 560 改成窗口高的一份额，客户区的形状可锁在 1.6:1，设置页「界面」卡上多了那两个开关。**最大化和 Windows 贴边仍按显示器的形状来**，那是明说过留下的口子。
- **上次插进来的一件（**做完了**，见下面「集页和季页的头图改用剧集那一层的图」那一节）**：「集页面要用这个剧的背景图或缩略图」—— 集页和季页头上那张铺满页面的图改成先要**剧集那一层**的背景图／缩略图，退到最后才用这一集自己的画面。
- **最近插进来的一件（**做完了**，见下面第一件活）**：「太黑了都看不清背景」—— 头图罩子最浓那一档和它底下那段尾部，从满黑改成七成二的黑（`DetailHero.ScrimCeiling`），那张铺满页面的剧照在片名、音轨和剧情说明背后一直看得见。
- **上次插进来的一件（**做完了**，见下面第二件活）**：「把那条线下移到我画红线的地方，然后去掉音频字幕还有剧情说明周围那一圈的边框和背景，改之前先备份」—— 头图那片暗色往下延到剧情说明的下沿，正文那张纸从那儿才开始；音轨／字幕、剧情说明、下一集三块外面那一圈框和底色全撤掉。备份：`C:\Users\89400\EmbyNian-backups\EmbyNian-20260831-0451-详情页那条线下移前.zip`，323 个文件 + 1 个说明，97.8 MB。

**用户一口气提的那十条全部做完并复验**（2026-08-30）。原文照抄，别改写：

1. ~~去掉窗口标题的 EmbyNian，参考上图新增按键，第一个图标折叠侧边栏、第二个设置（点击后弹出设置窗口）、第三个搜索（新增点击后进入搜索页面，直接在当前窗口跳转）、第四个后退、第五个前进~~（**做完了**，见下面那件活）
2. ~~用户和媒体服务器去掉头像，然后移动到窗口左下方下方，，诊断和服务器移动到设置里~~（**做完了**，同上）
3. ~~屏蔽媒体库里音乐的内容~~（**做完了**，同上）
4. ~~把媒体的徽标、缩略图、横幅图、艺术图、背景图融入对应媒体的 ui 界面~~（**做完了**，见下面那件活）
5. ~~参考 `C:\Users\89400\Desktop\主页轮播大图版-misty-4.9.css` 给主页轮播功能~~（**做完了**，见下面那件活）
6. ~~点击主页或者左侧的家园图标后，要直接回到主页界面~~（**做完了**，同上）
7. ~~在播放页面的右上角新增字幕字体的选择输入栏（要支持搜索）~~（**做完了**，见下面那件活）
8. ~~使用 `<WebView2>` 控件将 `http://192.168.31.230:8896/web/index.html#!/dashboard` 嵌入到设置页面中~~（**做完了**，见下面那件活）
9. ~~显示进度条的时候不需要同步显示音量条~~（**做完了**，见下面第二件活）
10. ~~加大音量条的尺寸，显示方式改为淡入淡出，鼠标指针越接近右边的中心显示越明显~~（**做完了**，同上）

**第 1 条里的「参考上图」那张图我看不到**（上下文里没有图片），五个按键是照文字实现的：折叠侧边栏、设置（弹窗）、搜索（新页面、当前窗口内跳转）、后退、前进；标题文字「EmbyNian」照要求删掉。这件事已经在交付第 1 条时告诉了用户 —— 如果后来那张图补上了、和实现不一样，改的是图标和顺序，几何和洞由 `ProbeTitleActions` + `ReportTitleBarHole` 兜着。

十条走的顺序是用户交给我定的（「你来决定先后顺序就好」），最后两条按宣布过的顺序先 4 再 5。`artifacts\publish\win-x64` 和那个 zip 都是第 5 条落地之后重新发布的，所以现在发布件里带着全部十条。

**自检里还剩两处已知偶发**：`屏幕像素 — 被遮挡`（别的窗口正好压在采样点上，重跑就过）—— **现在自检默认开在副屏，这一处基本不再发生**，见下面那件活；这个窗口里实测过两面：`--screen 1` 那一遍正好被用户自己的 mpv 压住采样点、红了，同一份构建默认开在副屏那几遍全绿。（这一关的第二个采样点后来从「内容宽度八分之一」改成「贴左边 24 像素」，因为侧边栏改成默认收起了，见归档里「侧边栏默认收起，那一排图标让开窄条」那一节。）另一处是 `鼠标真等两秒就藏`：判的是「静止两秒」，所以那两秒里真有人碰了鼠标它就红，**窗口化那半和全屏那半都会**（报告自己会写出那两秒里进了几次指针事件 —— `XAML 事件 2 次` 或者 `轮询问出 1 次移动` 就是有人碰了），重跑即过；2026-08-30 遇到过两次，第 5 条那一轮红在全屏那半，换图标那一轮红在窗口化那半。它的另外两条腿早先修掉了，见下面「Emby 自己的网页控制台」那件活的末段（`SetCursorPos` 被别的前台窗口拒掉时整条腿只作参考；`GetCursorInfo` 那句全局读数改成只打印不断言，换成断言「藏的时候让系统重新问了一次」）。`媒体信息表格` 那处**还会偶发，别当它已经死了**：2026-08-31 又红过一次，读数还是老样子 `10 行（已渲染 0，默认展开），首行「」`，同一份构建紧接着重跑就全绿。它的两种形状（`已渲染 0` 和滚到半路的 `10 行（已渲染 6…）`）各修过一轮 —— 见下面「标题栏那一排五颗按键」和「一口气四条」两节的末段 —— 这次是把看的眼数从四眼放宽到八眼，见归档里「集页和季页的头图改用剧集那一层的图」那一节的末段。

### 第五条「结构失衡」：三件全做完了

三件事，我定的顺序是**先小后大**：渐变档位只留一份（小、看得见的证据最硬）→ Shell 那层的单元测试 → 拆自检那两个大文件（改动最机械、最后做冲突最少）。三件的原文都搬进 [`docs/progress-2026-08.md`](docs/progress-2026-08.md) 了，一件一节，标题末尾写着「复核第五条第几件」：

- **渐变档位只留 Core 那一份（`8a83b3b`）** —— 头图罩子那五个停点从前在 XAML 和 Core 里各写一份，改一份忘另一份屏上就接出一道横缝；现在只留 Core 那张表，XAML 里那支渐变由构造函数填。测试 488 → 489。
- **Shell 那 35735 行第一次有单元测试钉着（`de3608a`）** —— 测试项目引不了 `-windows` 那个程序集，所以把四段真正纯的算术（卡片带翻页、换行、命令行开关、卡片尺寸）搬进 Core，在那儿测；WinUI 那一层照旧由自检钉着。测试 489 → 511。
- **自检那两个大文件拆成 partial（`d896c3e`）** —— `ShellSelfCheck.cs` 3602 行 → 九个文件，播放页自检 1783 行 → 七个文件。「一行代码没改」是逐行比过类正文证出来的，不是自称的：两边各差 0 行。

**判它成不成只能靠闸门** —— 用户看不见任何变化，所以四道全绿加自检报告逐行对得上，才算没把东西改坏。三件都是这么验的，三次都过了。**「每次都会变的行」那份名单现在是十项**：时间戳、两个 hwnd、字体过滤用了多少毫秒、光标形状句柄和线程号、能不能把指针挪到画面中心、静止那一段量出来是 2000 还是 2015 毫秒、任务栏等了多少毫秒、亚克力背后那个取样颜色、主页轮播那行的服务器读数，加上诊断页那两行的日志条数。下次逐行比报告时先照这份名单划掉，剩下的才值得查。

## 最近做完的活（一行一件，细节看 [`docs/progress-2026-08.md`](docs/progress-2026-08.md) 里的同名小节）

新的在前。归档里那一节的标题以这里的说法起头（后面可能多一句「复核第几条」），搜前半句就能落到原文。

- 2026-08-31 · 自检那两个大文件拆成 partial：3602 行 → 九个文件、1783 行 → 七个文件，正文一字节没动（`d896c3e`）
- 2026-08-31 · Shell 那层第一次有单元测试钉着：四段纯算术搬进 Core，测试 489 → 511（`de3608a`）
- 2026-08-31 · 头图罩子的渐变档位只留 Core 那一份，XAML 从它生成（`8a83b3b`）
- 2026-08-31 · 窄窗口下「媒体源／音频／字幕」排不下就换行，整排贴回左边（`734f752`）
- 2026-08-31 · 验证工具补上两个窟窿：`--scroll-half` 真的滚了、`--theme` 能把浅色主题拍下来（`4fad063`）
- 2026-08-31 · 发布件瘦掉 52 MB：拆开 Windows App SDK 那个总包（`32b76cf`）
- 2026-08-31 · 仓库瘦身、进度记录搬家、日志改成常开句柄（`a7cc3f9`）
- 2026-08-31 · 发布脚本加 `-NoArchive`：迭代时省掉那个没人看的压缩包（`272d00b`）
- 2026-08-31 · 修三处用户看得见的毛病：空封面、详情页慢、滚动卡顿（`2366f95`）
- 2026-08-31 · 整次 WinUI 3 重写入库：分支 `winui3-rewrite`、260 个文件（`bc8edbd`）
- 2026-08-31 · 头图那片暗色从满黑压到七成二，背景重新看得见
- 2026-08-31 · 详情页那道明暗分界下移到剧情说明底下，三块板去掉外圈
- 2026-08-31 · 集页和季页的头图改用剧集那一层的图
- 2026-08-31 · 窗口形状锁住，轮播那张大图不再越拉越被裁
- 2026-08-31 · 点封面不再先滑走一大段；标题栏那一条往下拉时渐变成正文的颜色
- 2026-08-30 · 一口气四条：齿轮回六齿细线、进详情页不再闪、背景改成固定的亚克力并铺进标题栏、片名头上那行眉字删掉
- 2026-08-30 · 重绘六个单元（词表、外壳、卡片和卡片带、详情页头图 + 主页轮播、设置页卡片 + 主题色板）与六套主题
- 2026-08-30 · 主页那条大图铺满窗口顶上那一整块，图源换成最近添加
- 2026-08-29 · 标题栏那一排五颗按键、账号挪到左下角、服务器和诊断进设置、音乐整片屏蔽、按家园必回主页
- 2026-08-29 · 鼠标静止两秒真的藏起来（关键是让系统重新问一次）
- 2026-08-29 · 壳层解耦、媒体库响应式与键盘／遥控器交互
- 2026-08-27～29 · 媒体库页和详情页对齐 Emby Theater（字母跳转条、视图切换、第二季卡带、推荐带、当前集高亮）
- 2026-08-25 · `PlayerPage.xaml.cs` 2717 行拆成一层视图加一个视图模型；拆掉 `ShellHost`

## 工作流的进展怎么记（2026-08-25 定的规矩）

原则：**工作流只在它能让代码更好的时候用，不为了快而用**。单纯图并行省时间的活直接自己做；能靠结构换到正确性的才值得散开 —— 一条结论交给互相独立的验证者去反驳、一个设计先要几种真的不同的思路、一次扫过比单个上下文装得下更多的材料。快只是副作用，不是理由。用了的话，编排者负责把结论写进这里，agent 自己不写。

- 起跑前先在这一节写一行「在跑什么、为什么、产物落到哪」。
- 结果落地就把**提炼过的结论**写进来，或者存到 `artifacts/` 再在这里留指针（就像上面那份 recipe）。原始记录不要往这里搬。
- 不要让并行的 agent 各自追加同一个文件 —— 九个写手交错写一份 Markdown 的结果不是进度记录。
- `resumeFromRunId` **只在同一会话内有效**。换了窗口，run id 就是死的；活下来的只有磁盘上的脚本、`journal.jsonl` 里已提交的结果，和写进本文件的东西。
- 教训是实打实的：`wf_8809d3d7-9eb`（journal 在 `.claude/projects/C--Users-89400/6e28ff25-…/subagents/workflows/wf_8809d3d7-9eb/journal.jsonl`）跑了 23 分钟、16 份记录，10 个 agent 里只有 1 个把结果提交上来 —— 就是被抢救成 `artifacts/shellhost-recipe.md` 的那份。另外 9 个全部 `started` 而没有返回，进程一换全部丢光，而当时本文件里一个字都没有。

## 未完成

- [x] 常用 mpv 参数改为下拉框、开关、滑块等专用控件 —— 设置页已用下拉框、开关和滑块承载字幕背景透明度、网络缓存等常用参数；“配置文件”分类支持 mpv.conf/input.conf 原始文本编辑、UTF-8/UTF-16/GBK 保留、重新载入、保存前备份与撤销未保存修改。由于播放启动使用 `--no-config` / `config=no`，这里的文件编辑明确作为外部配置查看与维护工具，不会覆盖客户端专用播放参数
- [x] 服务器发现 —— 登录页与服务器管理页通过 Emby UDP 7359 广播发现局域网服务器，支持网卡定向广播、HTTP/JSON 响应解析、地址规范化、按服务器 ID 或地址去重，以及超时/取消
- [x] 生成安装包 —— `tools/publish.ps1` 可重复生成 WinUI Shell 的自包含目录与 ZIP；传 `-Msix` 且机器有 Windows SDK 时额外生成未签名 MSIX
- [x] 清理迁移期 WinForms 外壳 —— WinUI Shell 已成为唯一桌面外壳，解决方案、README、冒烟脚本与发布路径均已切换
- [x] 自定义标题栏（`InputNonClientPointerSource.SetRegionRects`）—— 浏览态保留系统标题栏和缩放边框；播放态移除 `WS_CAPTION`、保留 `WS_THICKFRAME`，由 XAML 绘制可拖动顶栏和最小化/最大化/关闭按钮，画面从窗口顶端开始

## 继续开发入口

项目目录：`C:\Users\89400\EmbyNian`

**先读本文件顶部的「在途工作」一节** —— 那里写着手上没做完的活和它的入口。

验证用两条命令。注意 `dotnet` 不能直接用：`PATH` 上那个是 8.0.403，本项目要 .NET 10，SDK 在 `%USERPROFILE%\.dotnet\dotnet.exe`，没有进 `PATH`。

```
%USERPROFILE%\.dotnet\dotnet.exe build .\EmbyNian.sln --no-restore -m:1 -p:BuildInParallel=false -p:UseSharedCompilation=false -p:MSBuildNodeReuse=false
%USERPROFILE%\.dotnet\dotnet.exe run --project .\tests\EmbyNian.Tests\EmbyNian.Tests.csproj --no-build
```

必须单节点构建：本机 Windows SDK 的多节点 workload resolver 会让并行方案构建偶发无输出失败。改过界面再跑一遍 `--self-check --dump-ui` 看页面截图。**自检默认开在副屏**（这台机器上是右边那块竖屏），所以它不会抢走你正在用的屏幕；要看着它跑就加 `--screen 1`，只有一块屏的机器上这个默认值自动失效、照旧开在主屏。

改过颜色就用 `--theme <id>` 各拍一张，别只看默认那套：`--theme daylight` 是唯一的浅色，没登记进 `ThemeHost` 的画刷和系统自己画的那块标题栏只在它身上露出来。逐页拍照走 `tools\shot.ps1`（`-ExeArgs "--theme daylight --show-settings"`），自检自己只留一张截图。

做完的活在 [`docs/progress-2026-08.md`](docs/progress-2026-08.md) 里，包括十个阶段的完成清单和 2026-08-27 那次验收复核。
